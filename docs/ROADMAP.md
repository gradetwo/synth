# GS-1 项目路线图 / Roadmap

> 基线版本：**v1.8.0**（本文档保留原始规划）。
>
> **⚠ 实时进度以 [`docs/NEXT-PLAN-2.md`](NEXT-PLAN-2.md) 为准**（2026-09-14 起）。
> 本文档下方的「执行队列」（截至 v1.78.0）是**当时的原始规划与历史记录**，不再逐批更新；
> 当前状态看下面这张表，批次明细与债条目看 `NEXT-PLAN-2.md`。
>
> **进度看板（截至 v2.1.6）**
>
> | 阶段 | 项目 | 状态 |
> | :--- | :--- | :--- |
> | P0 | 参数平滑 / 主输出保护 / 撤销·重做扩展 / A-B 循环与节拍器 / 性能预算门禁 / 多浏览器 E2E / WASM 边下载边编译 | ✅ 已完成 |
> | P1 | Unison / 每声部 LFO 与调制扩展 / 扩展滤波（CMB·FRM）/ MPE / CC Learn / 微调律（含 .scl）/ 效果与混响（含波表、效果路由、卷积） | ✅ 已完成 |
> | P2 | 布局场景 / 多实例引擎 / 采样导入 / 多轨 + 时间线 / **任意拓扑节点图** | ✅ 全部完成（节点图 v1.72.0 起；图二期在 `NEXT-PLAN-2` 的 P7） |
> | P3 | 无障碍 / 更多语言 / 文档 | ⏸ 无障碍**经用户决定不做**（2026-09-14 再次确认）；文档 ✅（`docs/DSP-GUIDE.md`、`docs/LLM-INTERFACE.md`、`docs/notes/*`） |
> | P5–P8 | 工作流二期 · 声音二期 · 节点图二期与内存模型 · 工程与质量 | ✅ 随 v1.79.0–v2.0.x 交付（明细见 `NEXT-PLAN-2.md` 的版本表） |
> | P9 | 音频质量二期（度量卫生 / 带限 / 瞬态整形 / 每节点效果参数 / 图过采样 / 波表与采样插值器 / arena 扩容） | ✅ v1.98.0–v2.0.7 |
> | P10 | 创作与工作流二期（时间线多选 / 片段二期 / take 二期 / 工程管理 / 曲库合规） | ✅ 收官于 **v2.0.0** 里程碑 |
> | P11 | 工程与质量二期（wasm-opt / i18n 拆分与首屏预算 / 视觉与指纹门禁 / 平台矩阵 / 启动门禁） | ✅ v1.105.0–v1.113.0 |
> | P12 | 产品化与生态（教学与练习 / 内容包 / 分发与回滚 / SW 版本握手） | ✅ v1.113.0–v2.0.3（**P12.3 分享协作、P12.5 无障碍：不做**） |
> | P13 | 给其它 AI / LLM 的接口（MCP：认知 / 操作 / 渲染测量 / 浏览器层） | ✅ **全部完成**（P13.1 v2.1.0、P13.2/P13.3 v2.1.1、P13.4/P13.5 v2.1.2） |
>
> **版本里程碑**：**v2.0.0** = 「创作与工作流二期完成、可以日常当主力工具」；当前最新发布 **v2.1.6**（**Groove 集成的按帧寻址音符事件** `noteAt`/`noteOffAt` + 对外协议契约与防漂移门禁；**AudioWorklet 资源压缩后发运，首次访问少下约 20 KB**；工作台侧把一批 WebKit 假红变诚实、并修掉一条两次挡住发布的 PWA 离线假红）。
> 跨引擎（WebKit/Firefox）只在慢轨跑：WebKit 整包实测 **42.7 min**，按用户 2026-09-14 的 **20 分钟判据**
> 不进常规门禁（`NEXT-PLAN-2.md` §一.38；CI 的 push 触发跨引擎作业已删，改由 schedule 的 `nightly` 跑 `--all`）。
> **本地 WebKit E2E 统一走 `npm run test:e2e:webkit:wayland`（headless Weston）**，桌面会话用
> `test:e2e:webkit:desktop`；nightly 自动选择 Weston → Xvfb → headless 并在记录里注明。
>
> **执行队列（截至 v1.78.0 的历史记录；每批都要测试 + verify + 打包）**

| 批次 | 内容 | 状态 |
| :--- | :--- | :--- |
| 1 | A3 混响替换（阻尼 / 预延迟 / 宽度 / 湿声配平） | ✅ v1.14.0 |
| 2 | A2 lookahead 限制器 + 真峰值/响度/GR 表 + 导出 −1 dBFS 规范化 + A9 音质门禁 | ✅ v1.15.0 |
| 3 | A6 调制扩展：矩阵 8 槽 + 双极深度、AFTER/RANDOM/KEY 源、PAN/RES 目标、MIDI 触后 | ✅ v1.16.0 |
| 4 | A6 合成能力：**Unison 1–7 子声部 + SPREAD**（复音按层数折算） | ✅ v1.17.0 |
| 4a | A6 续：每声部（poly）LFO + **RETRIG / ONE SHOT** 模式 | ✅ v1.18.0 |
| 4b | P0.4 播放器：A/B 循环、节拍器、预备拍（量化/独奏随多轨批次） | ✅ v1.19.0 |
| 4b | A1 抗混叠：**重估后并入波表批次**——实测混叠 −57.7 dB（不可闻），而 2× 过采样预期仅再降约 6 dB 却要 +60% CPU；改由 A6.2 的 mipmap 波表振荡器（成本更低、抗混叠更好）统一解决 | 🔁 已改排期 |
| 5 | A4 每振荡器**真立体声**（PAN 不同即自动切换每振荡器独立滤波 + 等功率声像） | ✅ v1.20.0 |
| 5a | A4 续：**Comb 梳状谐振器**（Rust 每声部延迟线，噪声→有音高） | ✅ v1.22.0 |
| 5b | A4 续：**Formant 元音共振峰**滤波类型（三带通，A–E–I–O–U 滑动） | ✅ v1.28.0 |
| 5c | A4 续：SEM 风格滤波（现有 SVF 已覆盖 12dB 多模，优先级下调） | ⏸ 暂缓 |
| 6 | A6.2 **波表振荡器**（五张谐波表 + 每八度 mipmap 抗混叠，PW 选表） | ✅ v1.41.0 |
| 6b | A6.2 续：**单周期文件导入**（任意音频 → 自动找周期/平均 → mipmap 表，16 bit 持久化，导出同步渲染） | ✅ v1.45.0 |
| 7a | A5 续：**延迟重做**（乒乓交叉反馈、反馈环内阻尼、Rust 实现取代 Soundpipe） | ✅ v1.46.0 |
| 7b | A5 续：**效果链路可重排 + 并联送出**（同时覆盖 P2 的动态信号图：顺序即图） | ✅ v1.47.0 |
| U2 | **设置独立入口 + 分组重排**（顶栏同级入口；下拉按内容宽度，不再拉伸）；**iPhone/iPad 横竖屏 6 组 E2E** | ✅ v1.54.0 |
| U0 | 修复「重开标签页后无声音、启动键一闪而过」：启动门禁只在**引擎真正就绪**后收起（含 Chrome 自动播放策略下的回归用例） | ✅ v1.53.2 |
| U1 | **预设库底部排版重做**：等宽网格 + 字段成行对齐；设置随列表滚动，不再占用固定高度（含 E2E 防回归） | ✅ v1.53.1 |
| B1 | **双实例引擎**：第二套完整参数（消息传输 + 独立平滑）、每声部实例标记、Layer / Split / 力度窗口路由；默认单层，旧音色不变 | ✅ v1.53.0 |
| U5 | **英文排版加固**：固定槽位单行省略、顶栏/模块头可换行、过长英文缩写；新增「切英文后手机+桌面无溢出」E2E | ✅ v1.64.0 |
| U4 | **手机竖屏顶栏分行**（音色名完整一行、视图切换独占一行）+ **启动页重做**（logo/版本、更小按钮、缓动动画、尊重减弱动效） | ✅ v1.63.0 |
| U3 | **移动端体验**：手机竖屏音色横幅并入顶栏行、启动按钮重做、设置抽屉与播放器层控件的触屏尺寸（≥44px，含 E2E 断言） | ✅ v1.59.0 |
| C1a | **多轨基础**：格式 1 MIDI 保留轨道分层（轨道名 + 每轨音符），钢琴卷帘仍编辑合并列表 | ✅ v1.57.0 |
| C1b | **分层播放 + 每层静音/独奏**（播放中即时生效，单轨行为不变） | ✅ v1.58.0 |
| C1b2 | **分轨 MIDI 导出**（分层导出 format 1：指挥轨 + 每层一轨，带轨道名；单轨仍 format 0） | ✅ v1.60.0 |
| C1b3 | **每层音量**（滑杆；0 也保留音符，只降到最小力度） | ✅ v1.61.0 |
| C1b4 | **混音随曲目保存**（静音/独奏/每层音量跨重载与切歌保持；随曲库 schema 校验） | ✅ v1.62.0 |
| G2 | **卷积负载摊平**：每个 hop 的分段乘加分散到 hop 内的块（只有无法提前做的变换仍集中在边界块；基准新增 IR 行并断言其余 7 个块成本互相接近） | ✅ v1.69.0 |
| A1a | **节点图引擎（A1）**：6 个效果节点 + 干声源 + 输出汇的前馈图（每节点 2 路输入、带增益、可扇出），索引序即拓扑序、回向连线忽略即无环；`FX_GRAPH=1` 时走图、默认仍走旧链，`gs_fx_graph_sync` 从旧链自动映射且**逐样本一致**（Rust + WASM 门禁） | ✅ v1.72.0 |
| A2a | **路由图编辑器（A2）**：画布（端口拖线 / 点输出再点输入 / 点线断开）+ 列表视图（手机默认，键盘可达）、节点内联（效果类型/开关/并联/混合/增益）、三处入口、随音色保存（含 E2E 与手机视口） | ✅ v1.73.0 |
| B5 | **整首编曲随分享码**：曲目以 MIDI 字节随码传输、层混音一并带上；过长自动降级为 `.gs1song` 文件（可被预设库导入）；顺带修复「外部改混音后层条不刷新」 | ✅ v1.74.0 |
| A2b | **路由图卡片可拖动摆放**（位置进工作区、连线跟随、重置布局；参数批量提交减少重绘） | ✅ v1.76.0 |
| A2c | **连线的可视化编辑与键盘可达**：点选连线（误点不断开）、连线上直接调增益、非单位增益显示百分比标签、端口键盘操作（Tab + 回车）、连线本身可聚焦并用回车选中（含 aria-label 说明来源/目标/增益） | ✅ v1.77.0 |
| B6 | **分享码压缩**：带曲目的链接先用 `CompressionStream('deflate-raw')` 压缩（五分钟曲目 36,390 → 1,516 字符，约 96% 更小），无该 API 时回退明文；纯音色码与 `.gs1song` 格式不变，旧链接仍可读 | ✅ v1.77.0 |
| A2d | **效果节点多实例**：算法混响与四个插入效果**每个节点一套状态**（C 桥接层加 slot 维度、混响改为每节点一份）；延迟/卷积因内存上限仍单实例，第二个同类节点直通、编辑器标「已占用」并禁用；修掉「同一效果两个节点共用状态」的隐患 | ✅ v1.78.0 |
| C40 | **片段编排二期（P10.2）**：①**片段内直接编辑**（泳道与卷帘共用一份文档，P10.1 的批量编辑直接生效，`commit` 一次=一个撤销步；**不改源层 take**）；②**跨层复制片段** `copyClipToLayer` + `foldClipsInto`（**音高恒等**：clip 存绝对音高、层决定音色）；③**片段模板** `src/midi/cliptemplates.ts` + 工作区 `LayoutState.clipTemplates`（照 P7.3，不进分享码）。两条路**结构性**汇到 `clipFromNotes`，测试两条都真跑：`expect(byHand.notes).toEqual(copy.notes)`、`expandClips` 结果逐位相同，含 repeat+窗口裁切的折叠路也逐位不变；**不共享状态**用「不是同一数组/同一对象 + 改一边另一边不变」断言，模板用「就地改字段」这种浅拷贝必败形式；修掉折叠时**多减一次起点**的语义 bug（音符整体后移 0.5 s）。`fold()` 现在也可撤销。验收：Rust 225 / Vitest **449（57 文件）** / E2E clips+roll+takes **24 passed** / visual **10 passed**；dist 1550→**1562 KB**（wasm、CSS 未动）。**同时**恢复我在 v1.108.0 误删的 `verify:presets:2x` 并强化 `verify:ci`（见 `docs/notes/release.md`） | ✅ v1.109.0 |
| C39 | **视觉回归扩容 + 预设指纹二期（P11.3 + P11.4）**：视觉新增 **28 张基线**（更新横幅 / take 行 / 滤波双级 SER·PAR / 2× LED / 图模板 select / 路由图调制线 × 深浅 × 手机·桌面），**总量 48 张**，阈值仍 0.05 色距 + 0.01 占比，mask 补 `.spec-body`/`.strip-scope`/`.strip-spec`；**自证①**：`.take-chip` 改 `#ff00ff` → 桌面 2262 px、手机 3104 px 红（归档 expected/actual）→ 还原后 9 passed。**⚠️ 暴露的真相**：`test:visual` 在 v1.107.0 开工前**本来就是红的**（基线录于 `a092c6a`，之后 SEM/双级/2×/take 行都改过界面而套件不在 CI），本批补录 10 张过期基线。预设指纹加 **`--oversampled` → `tests/preset-fingerprint-2x.json`**，全量 **81 × 2**（≈34 s），进 `verify` + CI + `verify-ci.mjs`，`--reason` 强制；**自证②**：pluck cutoff −10% → **1× 与 2× 同时红**，还原后两个模式都绿。`dist`/wasm 前后 **sha256 逐字节相同**（未触碰引擎） | ✅ v1.108.0 |
| C38 | **卷帘/时间线多选编辑（P10.1）**：选择集提成纯模块 `src/midi/selection.ts`（**23 条单测**）——点选/`Ctrl`点切换/`Shift`按 `(start,pitch)` 矩形并集/空白**框选**（用 `.roll-grid` 实时 boundingRect 换算，滚动缩放都参与）/全选/文档变化后剔除；**批量移动、复制粘贴、删除、缩放到同一结束拍、量化、力度**，每条都断言**恰好一个撤销步**（手势期间不记历史，pointerup 从拖动前文档重放）；**跨层拖拽选「明确禁止 + toast 原因」**（层自带 take/片段语义，搬层只能静默丢语义），单测断言不写穿到另一层；键盘方向键微移（Shift 八度）、手机「多选」开关 + 批量条（按钮 **≥36 px**、无横向溢出）；`roll.spec.ts` **18 passed**（原 12 条未破 + 新 6 条），takes/clips/fxgraph/oversample 22 passed，`npm test` 437；dist 1538→**1550 KB**（`wasm` 逐字节未动） | ✅ v1.107.0 |
| C37 | **图模式过采样 + 逐节点 PDC（P9.4）**：`node_oversampled = Drive`（线性节点只补偿）；节点索引即拓扑序做单次前向 PDC（`lat(v)=max(max_输入 lat, own(v))`、`w(edge)=M−lat(src)`，**干声源→输出汇这条直通支路也必被延迟 M**），每条输入边独占 31 样本历史；**踩坑**：第一版把 2× 节点的往返**算了两遍**（节点内部干/湿对齐 + PDC 输入权重）⇒ 干腿 +62/湿腿 +31 ⇒ 31 样本错位交叉淡化 ⇒ **图 2× 比 1× 还差 25 dB（−33.0→−7.1）**，而链模式同代码下降 26 dB；误判方向是「问题在节点之后」（把真机节点输入喂回两条路径时二者逐位相同），真正线索是**节点混合后的输出**。修后真 wasm：图 DRIVE **−33.43 → −59.54（−26.11 dB）**、图+并联 EQ 支路 −25.42 dB；**图 2× vs 链 2× 互相关 1.000000**，图 2× vs 图 1× 0.998292 且 **最佳 lag = 62 = 上报延迟**（链同比较也是 0.998292 ⇒ 0.998 是带限方波的物理上限）。延迟上报 62 样本 / 1.29 ms；图 1× 与链模式 wasm **sha256 改前=改后**；三条红线未动；CPU p50 1099→1325 µs（1.21×）；dist/threshold 1533→**1538**、74→**75**。发现既存 bug：**节点参数区与其它参数编号重叠**（`FX_NODE_OUT_GAIN+1 == FX_DELAY_MIX` 等，宿主无法清干净 6 个节点），登记待修 | ✅ v1.106.0 |
| C36 | **`wasm-opt -Oz` 接入（P11.1）**：binaryen 132 进 **devDependencies**（构建期，不进产物），`build-wasm.mjs` 对**两个核**跑 `-Oz --all-features`，**幂等且永不失败**（取不到二进制/执行失败/产物过不了 `WebAssembly.validate` 都打印原因并保留未优化模块；降级后 dist 会被 1530 阈值挡红，是有意告警）；实测 **raw −31.2%/−35.0%、gzip 只 −0.9%/−1.0%** ⇒ **dist 1718.6 → 1523.1 KB（−195.5 KB）**，把 P9.2/P9.1c/P9.3 三轮「P11.1 买回来」的承诺一次结清；三红线逐字不变，且优化前后**两核 × 1×/2× 各渲染 96 000 样本逐字节相同**（等价优化）；新增 `verify-wasm-features.mjs` 钉「SIMD 核有 v128 / 标量核 0 / 两核 0 relaxed-simd」进 `test:wasm` 与 CI；阈值 `total` 1720→**1530**、`wasm` 74→**73**（gzip 是刻意收紧，如实注明）；更正计划里错的「gzip ≥10% 否则放弃」判据 | ✅ v1.105.0 |
| C35 | **录音 take 二期（P10.3）**：①take **重命名上 UI**（按钮/双击进入，Enter 提交、Esc 取消，手机 ✓/✕；`renameTake` **一个撤销步**且落盘）；②**A/B 试听**（按钮 + 键盘 `a`/`b`，真换内容且**保持播放位置**，停止回 A 并 toast）；③**合并策略可选**：`union`（原行为，默认）与新增 **`overwrite`**（同时间窗新 take 覆盖旧，边界相接算清空；并诚实说明直线式 overdub 下两者相同，只有分支历史可区分）；④**修掉 clips×take 语义歧义**——选「明确禁止 + 提示」并给出字段级依据（`MidiClip` 无 per-clip take 链接、重建会静默丢弃用户对片段的编辑），折叠层切 take/合并被拒且 toast「take 仅作素材」，改名仍允许、录音仍保存；原先的**静默无效已用单测钉死**（`withTakes` 返回新 takeId 但音符仍是 clip 展开）；`npm test` 407、E2E takes 4 passed；dist 1712→**1720 KB**（+6.16 KB 全是 JS/CSS，wasm 逐字节不动） | ✅ v1.104.0 |
| C34 | **每节点效果参数（P9.3）**：效果参数从「按种类」改成「按节点」——`fx.ovr[node][slot]`（6 节点 × **4 槽共享池**）+ 哨兵 `FX_OVR_UNSET`，槽位含义由该节点当前效果种类决定（无「种类 × 节点」爆炸）；id 净增 **41**（24 槽 + 8 target + 8 depth + 1 src），`PARAM_COUNT` 183→224，四处同步；**未覆盖逐位等于旧行为**（`worstDiff === 0` + Rust `to_bits()` 对照），三条红线一字未动；新增 **8 条覆盖调制总线**（可同时调 8 槽，与 P7.2 并存）；分享码/`.gs1song`/图模板携带（旧码仍可读、模板仍不含混响模式/IR）；换种类语义定为「保留数值、按新种类重新解释」；UI 节点内联 + 390×844 手机 36 px；dist 1684→**1711 KB**、wasm gzip 72→**74 KB**（记账 rebase，注释更正「P11.1 只买回 dist」）；顺带修好三个既存 bug：**卡片拖拽从未生效**（`.fxg-card-title` 高度 0）、覆盖槽量程把 8000 Hz/16 bit 夹成 4、两条 E2E 固定像素几何 | ✅ v1.103.0 |
| C33 | **普通振荡器带限（P9.1b）**：把 P6.2b 的专用带限振荡器（朴素波形 + BLEP/BLAMP + 95 抽头 Kaiser 抽取、2× 域、含 P9.1c 的跨跳变修正）复用到锯齿/方波/三角，正弦/波表/采样保持原路径并补 **23.5 样本**延迟补偿；真 wasm 全键盘离格底 **−37.4→−100.8（锯齿）/ −39.0→−111.3（方波）/ −49.4→−72.9（三角）**，正弦不回退；代价振荡器 **2.55×**（16 声部）、整链 p50 260→1169 µs（44%）、dist 1678→1684 KB、wasm gzip 70→72 KB（记账 rebase，P11.1 买回）；81 预设指纹**全部显式重录**（有意音色改变，kind `sound`）；顺带修掉 `PhaseInc()` 单位错（整体高一个八度）与 `bench.mjs` 计时门禁的 `mean>450 µs` 静默失效（改 `cpuProbe()` + p50<60% + 超标≤2%）；门禁地板阈值收紧到 −68/−95/−105，初相断言改为离散度 <20 dB | ✅ v1.102.0 |
| C32 | **硬同步重启对齐（P9.1c）**：根因不是相位漂移（P6.2b 起主振 `phase_`/`phase_inc_` 已是 double、重启整周位置精确），而是 **`sync_kernel` 跨过 BLEP 残差自身在 `d=0` 的跳变做线性插值**——主振 `xm` 走进样本边界前最后一格（`d∈[−1/64,0)`）时会拿到反号近乎满幅的修正，每次重启 1 个过采样样本被整级错修，残余周期性爆到 **−33 dB**（触发量是 DaisySP 增量的 f32 量化）；修复为跨跳变那一格补 `v += f`（`+196 B/核` wasm，dist 阈值显式 rebase 1676→1678 KB，P11.1 买回）。修复后真 wasm **12/12 场景、396/396 个 4 s 窗全部 ≥ −60 dB（最差 −88.1）**，时域相关 0.9988+、峰值 ≤0.136、步进 ≤0.129，16 次 note_on 离散度 84→18.8 dB；`<3 dB` 离散度只达 3/12（三角），登记为技术债 | ✅ v1.101.0 |
| C31 | **音质门禁的度量卫生（P9.1a）**：`verify-audio.mjs` 弃用「精确 bin 相减」（两大数相减 → 纯正弦被读成 −84…−102 dB，且常触 `1e-30` 下限），改用 **7 项 Blackman-Harris 整 4 s 窗 + 每谐波 ±8 bin（±2 Hz）带外功率/总功率**；正弦断言收紧到 **< −105 dB**（实测最差 **−119.1 dB**）、新增同场景 8 次离散度 **< 1 dB** 断言（实测 **0.00 dB**）、首次记录锯齿/方波/三角真实离格底（−37.4/−39.0/−49.4 dB，P9.1b 收到 −60）；场景统一 `QUIET_PATCH` + 清调制矩阵 + settle 400；锯齿/方波新旧尺子逐格 ≤0.6 dB 证明「变的是尺子不是引擎」；**并推翻「硬同步 −68.7 dB 只是测度问题」的旧结论**——同一持续音内坏窗 −33 dB、≈24.5 s 周期，立为新批 P9.1c | ✅ v1.99.0 |
| C30 | **瞬态整形（P9.2）**：`fx_shaping.rs` 的双包络差检测器（200 Hz 整流平滑 + 60 Hz 快 / 4 Hz 慢追快、`2^(dB/6)` 夹 `[0,8]`），id 179–182、每节点状态、进信号链与路由图；实测 attack ±0.5 → ±3 dB、sustain ∓3 dB、mix0/中性逐位直通、持续音 THD 0.00；dist 阈值因本批显式 re-base 1672→1676 KB（P11.1 买回） | ✅ v1.98.0 |
| C29 | **带限同步振荡器（P6.2b 第三步）**：`gs_daisy.cpp` 的 minBLEP/minBLAMP 核（Blackman 窗化 sinc、static BSS）+ 专用朴素波形从振，同一 double 相位上同时补回绕与主振重启的残差，68 槽环形累加器延迟 32 个过采样样本使对称核因果；真 wasm 混叠 **−68.7/−68.6/−77.3 dB**（锯齿/方波/三角，原 −32.1/−33.5/−49.5），时域相关 0.9995+；门禁与 Rust 单测改为断言 ≤ −60 dB；非整数比残差与窄脉冲边界已记录 | ✅ v1.97.0 |
| C28 | **体积与启动预算（P8.5）**：图标 PNG24→PNG8 **−48.97 KB**（RMSE ≤0.10%）、dist 1711.7→**1662.8 KB**；阈值**收紧**（总量 1672、首屏 JS 134、CSS 21、WASM 70 KB）；新增**首屏可交互时间** E2E 门禁（3200 ms，测量点含 FCP，实测 2096/2450 ms）；实测证明 `songs.ts` 懒加载是负收益（dist 净 +93 B、首屏 gzip 仅 −3.7 KB 却要异步化初始化），理由与「还能怎么省」见 `docs/notes/bundle-budget.md` | ✅ v1.96.0 |
| C27 | **图模板（P7.3）**：`src/state/fxtemplates.ts` 把「图 + 节点/连线/增益」存进**工作区**（不进音色/分享码），49 个 id 白名单 + 逐条夹取（排除混响模式/IR/效果 mix/调制边），`applyFxTemplate` 一次提交；内置 5 例（经典串联/双延迟/并行混响/失真分路/空图）；单测 10 条（含「非白名单 id 逐位不变」）+ E2E 2 条；Vitest 391、`verify` PASS、dist 1711.2/1712 | ✅ v1.95.0 |
| C26 | **图内调制（P7.2）**：id 167–178（4 条边 × SRC/DST/DEPTH）+ 18 个节点增益；LFO1/LFO2/ENV 源卡片与虚线调制边（键盘可达 MOD 条）、深度 chip；每块解算 `clamp(base+Σ depth·src, 0, 4)` 并过 ~20 ms 平滑；**无 live 边时零额外算术、深度 0 逐位直通**；与 8 槽矩阵目的地不重叠、并存；Rust 207 + Vitest 376（含频域边带）+ E2E 11；`test:dsp`/81 指纹未动；ABI 仍 8 | ✅ v1.95.0 |
| C25 | **延迟/卷积多实例（P7.1）**：`IrSpectra` 只读共享（1.21 MB）+ 每节点 `Convolver`（877 KB）、延迟每条 768 KB 池（`gs_init` 预留）、卷积缓冲首次导入 IR 时分配；池上限 **2 延迟 + 2 卷积**（3+3 会击穿 `bench:long` 的 512 KiB arena 断言）；第二同类节点不再直通、第三个在编辑器禁用并给原因；单测含两实例逐位独立与卷积尾部不串音；`bench:long` arena 绿、`verify-wasm` 池断言、`test:dsp`/81 指纹未动；ABI 仍 8、无新参数 id | ✅ v1.94.0 |
| C24 | **过采样开关（P6.5）**：`OVERSAMPLE`（166，默认关）把每声部滤波链与 FX DRIVE 插入放到 2× 下运算，63 抽头 Kaiser 抽取/插值共用系数表，延迟补偿 31 样本；真 wasm 实测混叠 **−26.1 dB**（1× −33.4 → 2× −59.5）；DSP 指纹**双基线**（`dsp-baseline.json` + `dsp-baseline-2x.json`）进 verify/CI；成本 16 音 442→829 µs（16.6%→31.1%）；已知边界见 `docs/notes/oversampling.md` | ✅ v1.93.0 |
| C23 | **位粉碎 + 塑形 EQ（P6.4）**：`fx_shaping.rs` 的 BitCrusher（量化/采样保持/前后两级一阶 LP 抗混叠）与 ShapingEq（RBJ 三架），id 152–165、每节点独立状态、默认关；实测镜像 −14.1 dB→AA 再 −20.1 dB、干湿 0 时 −152.1 dB、EQ 三段 ±1 dB 内；Rust 10 条 + 门禁 9 项 + E2E；`test:dsp`/81 预设指纹未动；dist 总量阈值 1700→1712 KB（P8.5 压回） | ✅ v1.92.0 |
| C22 | **录音 take 与叠加（P5.4）**：`MidiTake`+`MidiSong.takes/takeId`，叠加=每次录完新建 take（非破坏、可切回），同音高 50ms 重击替换、每层上限 8 退休最旧；`song.notes` 仍是展开结果所以播放/卷帘/导出/分享自动跟随当前 take，分享码带全部 take；schema 3→4 加法式 + 迁移样本；14 条单测 + E2E（录两次→切→重开→合并→删除）；顺带把最新更新记录拆到 `changelog-head.ts`，**首屏 JS 165.0 → 127.3 KB** | ✅ v1.91.0 |
| C21 | **双滤波串联/并联（P6.3b）**：参数 `FILTER_ROUTING`（146）/`FILTER2_TYPE…DRIVE`（147–150）/`FILTER_BLEND`（151）；第二级是每声部每侧一份独立 12 dB SVF，串联幅频相乘、并联 `(1-b)A+bB`；`OFF`（默认）完全不进入、旧补丁逐位不变；第二级类型映射走 `second_stage_id()`（comb/formant→低通，`bridge_id()` 带护栏断言不能复用）；Rust 13 条（端点逐位/加权和/乘积/斜率/无爆音）+ 真 wasm 门禁（乘积 1.02 dB、−26.6 vs −53.8 dB、blend 单调、步进 0.162）+ E2E + 手机尺寸 | ✅ v1.90.0 |
| C20 | **SEM 连续多模滤波（P6.3a）**：新增 `SEM` 档（12 dB/oct）+ 参数 `FILTER_MORPH`（id 145）；一个 SVF 的四规范点分段线性混合（0=L、1/3=B、2/3=L+H 精确陷波、1=H），Rust→C 走显式 `bridge_id()`；UI 只在该档显示 MORPH 旋钮、曲线随 morph 画；频域（Rust 8 条 + 真 wasm 门禁：−11.68/+12.19 dB、带通中心 +17 dB、陷波 −32.4 dB）与时域（急变不爆音、扫动有界）双域断言 + 2 条 E2E；`test:dsp` 与 81 个预设指纹一字未动 | ✅ v1.89.0 |
| C19 | **预设指纹（P8.3）**：`scripts/verify-presets.mjs` —— 81 个工厂预设各过同一句固定乐句，录 rms / peak / 左右平衡 / 24 个三分倍频程频段（Goertzel），基线 `tests/preset-fingerprint.json` 每预设一行；`verify:presets` 进门禁与 CI（`verify-ci.mjs` 守卫）；变化必须 `presets:update --reason "…"` 显式确认；实测 5 % 参数改动即失败、1 % 微调通过、噪声底 0.000 dB（见 `docs/notes/preset-fingerprint.md`） | ✅ v1.88.0 |
| C18 | **视觉回归（P8.2）**：`e2e/visual.spec.ts` —— 模块网格 / 信号流 / 钢琴卷帘 / 播放器 / 启动页 × 深/浅配色 × 手机 390×844 / 桌面 1440×900 共 20 张基线（无损 PNG 入库），阈值比较（色距 0.05 + 占比 0.01），失败留下 expected/actual/diff 与像素差异数；含套件内自证用例（刷品红必须被拒）与真实标定（16/255 色差在默认阈值漏过、现阈值拦下 52 585 px）；可选套件 `npm run test:visual`（CI 跳过，理由与实测坑见 `docs/notes/visual-regression.md`） | ✅ v1.87.0 |
| C15 | **解析器模糊测试**：8 类外部输入（分享码、补丁文件、`.gs1song`、MIDI、Scala、WAV、存储波形、Web MIDI 消息）各 10 000 个固定种子的随机/变异输入，断言「要么拒绝、要么合法、不抛未文档化异常、不超时」；修掉 5 个实际问题（MIDI 读取越界、损坏字节造出非法音符/力度、tempo=0 得到 `Infinity` BPM、谎报轨道数导致按声明分配（50 字节文件 25 ms → 0.11 ms）、`decodeMidi` 非 7 位输入越界），补丁文件解析抽成纯函数 `parsePatchFile` 并加上限；顺带修掉「导入的补丁文件在顶栏不显示名字」（新增补丁文件导入的 E2E） | ✅ v1.81.0 |
| C17 | **速度与拍号（P5.3）**：`src/midi/tempo.ts` —— 以**拍**为界的多段 BPM + 拍号；`beatsToSeconds`/`secondsToBeats` 在段边界精确（±1 ms）、`gridStepAt` 让「小节」网格随拍号（3/4 里一小节 = 3 拍）、`barBeatAt` 给「小节.拍」读数；节拍器改为按**拍**步进（每拍经地图换算成时间），重音与预备拍同源；播放器面板新增速度段编辑行（BPM + 拍号 + 增删）与 `小节.拍` 读数；SMF 导入/导出读写 tempo(0x51) 与拍号(0x58) 事件、按地图做秒↔tick 换算（单速曲目字节不变） | ✅ v1.86.0 |
| C16 | **片段编排（P5.2）**：`src/midi/clips.ts` —— 片段=自己的音符 + 时间线位置 + 循环窗口 + 铺排次数，`song.notes` 永远是**展开结果**（播放器/卷帘/分享码/导出无需认识片段）；每层独立编排（没片段的层照原样播放）；窗口边界裁切、重叠相加、上限（窗口 600 s / 64 次）；层条上的片段轨道（折叠/移动/复制/窗口/铺排/删除 + 循环分隔线）与卷帘的片段选择器；曲库 schema **3**（加法式升版，v2 曲目无片段读回行为不变，迁移样本在 `migrate.test.ts`） | ✅ v1.83.0 |
| C14 | **时间线直接编辑音符**：缩略时间线加「编排 / 音符」两种模式（音符模式：点选、拖动改位置、拖右缘改长度、双击删除、1/16 吸附、边界限制，外加一排微调按钮给手机用）；文档与撤销历史搬到共享会话 `src/state/roll.ts`，与钢琴卷帘**同一份数据、同一套撤销**（卷帘里 Ctrl+Z 能撤销时间线上的改动，应用级撤销也能，因为每次编辑都进 `store` 历史）；编辑随曲目即时落盘，内置曲目自动存副本；顺带修掉「卷帘保存多轨文件会压成单轨」 | ✅ v1.80.0 |
| C11 | **界面每帧成本**：画布改为按需重绘（示波器/频谱/迷你波形/滤波曲线/电平表只在数据或参数变化时重画、渐变缓存、DOM 只在文本变化时写）、去掉 7 处全屏 `backdrop-filter`、启动页动画改用 opacity 与静态光晕；Chromium 空闲 7.5 → 60 fps（音频运行中同样 60 fps），本机全量 E2E 8.7 → 4.2 分钟，并新增「音频运行时 > 20 fps」的 E2E 守卫 | ✅ v1.79.0 |
| C12 | **本机 WebKit 标准跑法定为 headless Weston**（`npm run test:e2e:webkit:wayland`，比 Xvfb 快一倍、有 `/dev/dri` 时走 GPU），nightly 自动按 Weston → Xvfb → headless 选择并记录显示栈；同时更正早期结论：慢的根因是应用页每帧成本，不是内核（空白页三内核均 60 fps） | ✅ v1.79.0 |
| C13 | **一键发布**：`npm run release -- <version>`（版本与更新记录校验 → `verify` → E2E → 打包 → 部署 → 线上 hash 核对并重试 → 打 tag，失败即停，`--dry-run` 可演练）；同一校验作为 `verify:release` 进本地门禁与 CI，杜绝「版本与更新记录脱节」进 main（见 `docs/notes/release.md`） | ✅ v1.79.0 |
| C10 | **体积继续拆分**：播放器面板改为按需 chunk 且首次打开才挂载（初始 JS 153.3 → **150.8 KB gzip**） | ✅ v1.78.0 |
| C9 | **门禁在繁忙机器上不再假红**：`bench` 按 `loadavg` 判定宿主是否过载（过载只报正确性、跳过计时并说明原因）；Playwright 默认超时 45→60 s；WebKit/Firefox 项目开 `reducedMotion` + 单独超时 + 重试；修掉两个跨引擎测试缺陷（Chromium 专用启动参数害 WebKit 起不来、浮动键盘挡住模块按钮） | ✅ v1.76.0 |
| C8 | **真正的夜间跑**：仓库新增 `nightly` 作业（`on.schedule`，`verify-ci` 守住它）+ 本机 `npm run nightly`（WebKit 核心子集 + `xvfb-run --headed`、锁、日志轮转、`docs/notes/nightly.md` 记录）+ systemd 用户定时器模板 | ✅ v1.76.0 |
| C7 | **本机 WebKit 可跑**：依赖装好后定位到「headless WebKit 不触发 rAF → Playwright 点击稳定性检查挂死」，给出 `xvfb-run … --headed` 跑法（`npm run test:e2e:webkit:headed`），记录在 `docs/notes/compat.md` | ✅ v1.75.0 |
| C6 | **工程项收尾**：`cargo clippy` 门禁（correctness/suspicious/perf，CI 同步）、`npm run bench:long` 60 秒长时基准（含内存/arena 断言）、更新提示显示本次更新内容 | ✅ v1.75.0 |
| G4 | **Firefox 启动卡死修复**：`resume()` 可能永不 settle，启动改为限时等待 + 「点按恢复」提示（含回归 E2E 与 `docs/notes/compat.md`） | ✅ v1.72.0 |
| G3 | **持续负载基准**（`npm run bench`，写入 `docs/notes/performance.md` 并进门禁；当前均值 9.1% 预算） | ✅ v1.66.0 |
| B4 | **分层音色进入预设/导出/分享**（带层才带路由；无层不覆盖对方的层） | ✅ v1.65.0 |
| C1b5 | **每层声像**（逐音符立体声位置：引擎按声部带 pan，滑杆 + 随曲目保存 + 测试） | ✅ v1.70.0 |
| C1b5b | **导出带上层混音**：MIDI（静音/独奏/音量→力度/偏移→时间/声像→CC10）、WAV/MP3（同一套混音）与播放完全一致 | ✅ v1.71.0 |
| C1c | **时间线第一步**：每层缩略时间线（音符块 + 播放头，纯展示） | ✅ v1.67.0 |
| C1c2 | **时间线交互第一步**：点按定位播放位置 + 拖拽整层时间偏移（随曲目保存，测试齐全） | ✅ v1.68.0 |
| C1c3 | 时间线续：拖拽裁剪音符长度、与钢琴卷帘双向联动 | ⏳ |
| B3 | **分享链接携带第二层**（带层时附带、不带时长度不变且不覆盖对方的层） | ✅ v1.56.0 |
| B2 | **双实例界面**：设置里选 1/2 决定面板编辑哪一层（store 层做到实例感知，所有面板自动跟随）、按键分配单层/叠加/分区 + 分界音、工作区持久化、E2E | ✅ v1.55.0 |
| W0 | 修复离线缓存把托管重定向（`/index.html` → `/`）交回浏览器导致的「第二次访问打不开」（Safari/Chrome） | ✅ v1.52.1 |
| W1 | **曲库持久化**：导入的 MIDI 与录制片段写入本机（schema 版本化、上限 12 首、超限仅本会话） | ✅ v1.52.0 |
| A | **采样导入（SMP 音源）**：按八度 mipmap 抗混叠变速播放、一次性/循环/乒乓、ROOT 与循环点、16 bit 持久化 + 导出渲染 | ✅ v1.50.0 |
| F | **存档 schema 版本化与迁移**（预设/分享链接/工作区/场景/波形/IR；更新版本存档回退并保留副本） | ✅ v1.49.0 |
| 7c | A5 续：**卷积混响（IR 导入）**：分段卷积（1024 采样/段，频域乘加 + 输入谱延迟线）、单位能量归一化、混响单元 ALGO/IR 双引擎 | ✅ v1.48.0 |
| 8a | A7：**微调律**（平均/纯律/毕氏/中庸律，逐键音分表） | ✅ v1.31.0 |
| 8b | A7 续：**CC Learn**（CC→控件映射，持久化） | ✅ v1.32.0 |
| 8c | A7 续：**Scala .scl 导入**（任意音数/周期，非 12 音不折算） | ✅ v1.33.0 |
| 8d | A7 续：**MPE 每音弯音**（通道→音符路由、关闭时复位弯音） | ✅ v1.34.0 |
| 9 | P0.4 播放器增强：**A/B 循环 + 节拍器（含预备拍）** ✅ v1.19.0；录制量化、轨道静音/独奏随多轨批次做 | 🔄 部分 |
| 9b | P0.4 续：**录制量化**（1/16 · 1/8 · 1/8三连 · 1/4，仅作用于新录制） | ✅ v1.39.0 |
| 9a | **P0.3 撤销/重做扩展**：布局（折叠/视图/信号流位置/重置）、用户预设删除、片段删除全部可撤销；历史记录会话初始状态 | ✅ v1.24.0 |
| 10a | P2：**布局场景**（保存/一键切换工作区；偏好不受影响） | ✅ v1.40.0 |
| 10b | P2 核心：动态信号图（真正可重排）、节点内联编辑、多实例引擎 | ⏳ 下一批 |
| 11a | **音频设置面板**（状态/采样率/延迟/内核/负载 + 复音上限）与**启动预热** | ✅ v1.26.0 |
| 11b | 音频设置：**输出设备选择**（Chromium `setSinkId`，不支持时明确提示） | ✅ v1.42.0 |
| 11 | P0.5 多浏览器 E2E（配置已就绪，本机缺系统库）、P0.6 进一步 streaming | 🔄 部分 |
| 12a | **Web MIDI 输出**（演奏转发到外部设备，关闭时发送 all-notes-off） | ✅ v1.43.0 |
| 12b | 多轨/时间线、采样导入、预设迁移 | ⏳ 下一批 |
| 13a | A8：**噪声类型**白/粉/棕（频域斜率验证：−0.07 / −3.08 / −5.99 dB/oct） | ✅ v1.35.0 |
| 13b | A8：**力度曲线**（线性/柔和/强硬，仅作用于实时演奏） | ✅ v1.38.0 |
| 13c | A8：**偷声策略**（已实现：优先偷"已释放且最轻"的声部，年龄仅作平局判定）+ 新增策略测试 | ✅ v1.38.1 |
| 13d | A8 剩余细节：滤波/调制微调 | ⏳ 下一批 |

**音质专项（A1–A9，v1.13.3 起）**
>
> | 项 | 内容 | 状态 |
> | :--- | :--- | :--- |
> | A1 | 抗混叠测量：C7 锯齿混叠相对信号 **−57.7 dB**（wasm）/ −150 dB（native，两者差异来自 FMA 与相位舍入，属次采样级差异，待过采样时一并复核）；DRIVE THD 1.1% → 4.2%（奇次谐波，属模拟味） | ✅ 已量化；过采样列为下一项 |
> | A2 | 主总线不再削波；**128 采样（约 2.7 ms）lookahead 限制器**真正生效（线性起音 / 150 ms 释放），末级为 0.82 以下逐位透明的软限制器；导出按 −1 dBFS 规范化 | ✅ 已完成 |
| A2b | 真峰值 / 短时响度 / 限制器衰减表（工作线程上报 → 监视器显示） | ✅ 已完成 |
> | A3 | 混响换为自研 Freeverb 结构（阻尼 / 预延迟 / 立体声宽度 / 慢速调制） | ✅ 已完成（v1.14.0） |
> | A4 | 每振荡器真立体声（每声部双滤波器） | ⏳ 待做 |
> | A5 | 效果并联 / 可重排、ping-pong 延迟、卷积混响 | ⏳ 待做 |
> | A6 | Unison、波表、扩展滤波、每声部 LFO、矩阵 8 槽与双极性、S&H/随机调制源 | ⏳ 待做 |
> | A7 | MPE、CC Learn、aftertouch、微调律 | ⏳ 待做 |
> | A8 | 噪声类型、偷声策略、力度曲线、LFO 重触发等细节 | ⏳ 待做 |
| A10 | **静音尾巴快速路径**（−54 dB 以下不过滤波）：电钢/铃声类长释放音色在密集曲目下的负载从 24–27% 降到 11% | ✅ v1.23.0 |
| A11 | 过载保护按"是否已错过截止时间"反应，监视器显示 DSP 负载 | ✅ v1.23.0 |
| A18 | **振荡器相位累加改双精度（P6.2b 第一步）**：f32 相位累加器每步 ~1e-7 周期的舍入会形成每个分音周围的裙边，纯正弦的整秒离网能量 −66 → **−87 dB**；DSP 指纹不变（零兼容成本），新增「纯正弦离网能量 < −80 dB」回归测试；并把离网度量做成可用：**起振瞬态会让限幅器慢增益被量成 −69 dB 底噪，音符稳定后底噪降到 −90 dB**；稳定测量下未同步从振 −0.0 dB、硬同步锯齿 **−32 dB**（普通锯齿本来就 −39 dB）——**−60 dB 目标未达成**，剩余工作是把重启不连续点本身带限（BLEP），见 `docs/notes/hard-sync-aliasing.md` | ✅ v1.85.0 / P6.2b 进行中 |
| A17 | **硬同步 + sub + 噪声混合（P6.2）**：`gs_voice_osc_sync_block` —— 主振（OSC 2）驱动从振（OSC 1）相位重启，**2× 过采样 + 95 抽头 Kaiser 低通（通带 19.2 kHz / 阻带 −72 dB）**降采样，重启点按**亚采样精度**对齐；每振荡器 sub（正弦，−1/−2 八度 + 电平）与全局噪声混合；三个开关默认关。验收：时域（周期相关系数 0.99 vs 未同步 0.24–0.50）+ 频域（非整数比时从振线让位 >20×、整数比不误判、sub 的八度与纯度、噪声宽带 +55 dB）在 Rust 与真 wasm 门禁双处断言；**「混叠 ≤ −60 dB」未达成也未断言**——离网度量的底噪本身就有 −40…−60 dB，原因与后续步骤见 `docs/notes/hard-sync-aliasing.md`，跟踪为 **P6.2b** | ⚠️ v1.84.0（一项待办） |
| A16 | **FM 相位调制与环形调制（P6.1）**：`gs_voice_osc_pm_block` 按样本**放置**载波相位（不是累加偏移——累加会变成对调制信号积分的另一种、亮得多的调制，本轮被「教科书频谱」单测当场抓出）；`OSC_FM`（平方曲线，满量程 2 个周期）与 `OSC_RING`（干湿交叉淡入，`sqrt(l1·l2)` 电平补偿）两个新参数默认 0；调制矩阵新增 `ModDst::Fm` / `ModDst::Ring`；验收：Rust 时域+频域（`sin(x+β sin x)` 的贝塞尔边带与解析解逐谐波比对、深度→边带数单调、环形调制的和/差频与载波抑制、波形零点数、unison 下稳定）+ `verify-audio` 8 项门禁（经真 wasm）+ 2 项 E2E（控件可达、分享码往返、矩阵可选） | ✅ v1.82.0 |
| A15 | **负载监控误报修复**（预热按音频时长 2 秒、连续块计数判定、均值慢升快降） | ✅ v1.34.1 |
| A14 | **wasm 全局构造函数隐患根治**（类内初始化 → 每次导出调用重跑 ctor；已去初始化 + 门禁防复发） | ✅ v1.32.2 |
| A13b | 波表抗混叠纳入门禁 | ✅ v1.43.1 |
| A13c | 混叠检查扩展至全部含谐波波形（锯齿/方波/脉冲/波表，−84～−156 dB） | ✅ v1.44.1 |
| A13d | 导入表纳入门禁：导入正弦谐波 −129 dB、导入锯齿 C7 混叠 −129 dB、与内置振荡器电平一致 | ✅ v1.45.0 |
| A13e | 门禁补齐：采样音高、IR 能量归一化（+2.2 dB）、效果链顺序确实改变声音（4.6 dB）、空链为精确旁通；参数 id 拼错立即报错 | ✅ v1.51.0 |
| A13 | **音质门禁时域+频域双检**：块边界咔哒扫描（含自检）、正弦纯度（谐波 −125dB / 宽带噪声 −97.6dB）、整曲频谱平坦度 | ✅ v1.32.1 |
| A12 | **低通滤波器改用自研 Rust 24dB 梯形**（原 vendored 版本在 wasm 下每块丢样本 → 单音刺啦）；混叠 −57.7→−130.2 dB、负载 19→8% | ✅ v1.30.0 |
> | A9 | 音质门禁 `npm run verify:audio`：满负荷和弦峰值/硬限幅占比、锯齿混叠（wasm 实测 −57.7 dB）、DRIVE THD（4.3%）、最坏块 CPU（19%）全部纳入 `verify` | ✅ 已完成 |

---

## 1. 现状盘点（v1.7.1）

**音频引擎（Rust → wasm32）**
- 减法合成：2 振荡器（6 波形、失谐、脉宽、声像）、Moog 梯形低通（24 dB/oct，Huovilainen 模型）+ SVF（HP/BP/NT，12 dB/oct）、双 ADSR、双 LFO、4 路调制矩阵、混响（Soundpipe `reverbsc`）、同步延迟、合唱/镶边/移相/过载。
- 工程特性：AudioWorklet 块处理（128–1024）、k-rate 参数、8 MiB 静态 arena 零分配、SIMD + 标量双版本运行时探测、Transferable ArrayBuffer 事件、负载过高按 4 步降复音。
- 16 复音、最旧/最弱偷声 + 8 槽待定队列无咔哒重触发、等功率声像。

**前端（React 18 + TS + Vite）**
- 三种工作区：模块网格、信号流画布、演奏模式（信号流承载）。
- MIDI 播放器：16 首内置曲目、SMF 导入、走带（循环/变速/移调）、现场录制、导出 MIDI/MP3（lamejs 懒加载）。
- 和弦识别（21 种模板 + 转位）、67 个工厂预设、A/B、撤销/重做、双语、PWA 离线 + 更新、响应式 + 触屏手势 + 触觉、内置双语指南（8 章）。

**质量门禁**
- 43 个 Rust 测试、~90 个 Vitest、23 个 Playwright E2E、WASM 双版本校验、DSP 基线、ESLint、GitHub Actions。

---

## 2. 已知问题与技术债

| # | 问题 | 影响 | 归属 |
| :-- | :--- | :--- | :--- |
| D1 | 参数为 k-rate 且直接写值，无平滑 | 快速拖旋钮可能有 zipper noise | 音频 |
| D2 | 主输出无限制器/软削波 | 多复音叠加可能硬削波 | 音频 |
| D3 | 撤销/重做只覆盖 patch 参数 | 布局/录制/导入不可撤销 | 状态 |
| D4 | 内置曲目含版权片段 | 分发合规风险 | 法务/内容 |
| D5 | `static mut ENGINE` 单例 + 固定 8 MiB arena | 无法多实例/多引擎对比 | 架构 |
| D6 | 信号流是固定拓扑的可视化 | 不能真正重排处理顺序 | 架构 |
| D7 | 无 MPE / CC learn，MIDI 输入较基础 | 外接控制器体验受限 | MIDI |
| D8 | E2E 仅 Chromium | Safari/Firefox 回归靠手测 | 测试 |
| D9 | WASM 用 fetch + instantiate，非 streaming | 首屏多一次往返 | 性能 |
| D10 | 画布动画在低端机可能吃帧 | 移动端续航/流畅度 | 性能 |
| D11 | 无视觉回归与性能预算门禁 | 视觉/性能回退不可见 | 工程 |

---

## 3. 规划总览

| 阶段 | 主题 | 预计版本 | 工作量 |
| :--- | :--- | :--- | :--- |
| **P0** | 音质与稳定性、关键体验缺口 | v1.8.0 | 1–2 周 |
| **P1** | 合成能力与演奏表达扩展 | v1.9.0 / v1.10 | 1–2 个月 |
| **P2** | 真正的模块化信号图与工作流 | v2.0.0 | 2–4 个月 |
| **P3** | 可访问性、国际化、分发与生态 | v2.1+ | 持续 |

---

## 4. P0 — 音质与稳定性（v1.8.0，1–2 周）

目标：把"能弹、好听、不崩"变成"专业可用"，并补齐工程门禁。

### 4.1 参数平滑（解决 D1）✅ 已完成
- 连续参数（cutoff、level、detune、mix 等）改为 **a-rate 线性/指数斜坡**：`AudioParam.linearRampToValueAtTime` 或在 worklet 内做 one-pole 平滑（推荐后者，保持 k-rate 接口但每块内插值）。
- 离散参数（波形、开关、同步）保持块速率跳变。
- **验收**：旋钮 0→100% 快速拖动无 zipper（频谱无周期性边带）；DSP 基线不回归；新增单元测试覆盖平滑系数。

### 4.2 主输出保护（解决 D2）✅ 已完成
- 主总线加软削波（`tanh`）与可选简易限制器（前馈、快攻慢放）。
- **验收**：16 声部 + 满共振 + 满过载下输出不超 0 dBFS 硬削；THD 在可控范围；A/B 对比听感不糊。

### 4.3 完整撤销/重做（解决 D3）
- 历史栈扩展为"命令"模型：参数、布局（顺序/折叠/信号流位置）、录制片段、导入、预设保存/删除均可撤销。
- **验收**：E2E 覆盖每种操作的 undo/redo；`Ctrl/Cmd+Z` 在任意面板可用。

### 4.4 播放器增强
- A/B 段落循环（拖选区间）、节拍器、录制量化（1/16/1/8/三连）、录制倒计时、轨道静音/独奏。
- **验收**：单测（量化/循环边界）+ E2E（录制→量化→回放）。

### 4.5 多浏览器 E2E（解决 D8）
- Playwright 增加 **WebKit** 项目（Safari 引擎）与 **Firefox**；关键路径（启动、发声、导入、信号流）三浏览器跑通。
- **验收**：CI 三浏览器绿灯；Safari 特有回归（`performance`、`inset`、drop-shadow）纳入用例。

### 4.6 加载性能（解决 D9）
- `WebAssembly.instantiateStreaming` + 失败回退；首屏骨架与进度提示；`wasm-opt -Oz` 与 `--strip`。
- **验收**：4G 首屏可交互 < 2s；WASM gzip < 200 KB。

### 4.7 性能预算门禁（解决 D11）✅ 已完成
- 脚本记录：WASM/JS/CSS 体积、首屏 TTI、单块 `gs_process` 耗时、16 声部 CPU 占用。
- **验收**：超阈值 CI 失败；每次发布生成性能报告。

---

## 5. P1 — 合成能力与演奏表达（v1.9–v1.10，1–2 个月）

### 5.1 Unison / 子声部
- 每个振荡器支持 1–7 个子声部，带失谐散布、相位散布、立体声扩散；supersaw 用 7×2 声部。
- **验收**：预设"Supersaw"听感达到商业参考；CPU 在预算内（可降子声部数）。

### 5.2 波表振荡器
- 单周期波表导入/内置库、线性插值、mipmap 抗混叠；波表位置可被 LFO/矩阵调制。
- **验收**：扫波表无混叠；导入 2048 点单周期文件。

### 5.3 滤波器扩展
- 新增 SEM（12 dB 多模）、Comb、Formant；每声部可串联/并联双滤波。
- **验收**：DSP 稳定性测试（高 Q 不自激失控）；预设展示。

### 5.4 调制扩展
- 新调制源：S&H、随机、包络循环、aftertouch、力度曲线；LFO 相位重置/单次/同步；矩阵扩到 8 槽 + 双极性。
- **验收**：矩阵路由可视化连线；单元测试覆盖双极性与深度换算。

### 5.5 效果扩展
- 效果并行/串联路由与顺序拖拽、卷积混响（IR 导入）、更多失真类型、主限制器。
- **验收**：IR 导入 < 2s；效果顺序切换无爆音。

### 5.6 MPE 与 MIDI Learn（解决 D7）
- MPE（每音弯音/压力/滑音）；CC 映射可编辑并持久化；MIDI learn 一键绑定。
- **验收**：外接 MPE 控制器 E2E；映射导入导出。

### 5.7 微调律
- Scala `.scl`/`.kbm` 导入、内置平均律/纯律/五度相生律。
- **验收**：音高偏差在 ±1 音分内；键盘映射正确。

---

## 6. P2 — 真正的模块化信号图（v2.0.0，2–4 个月）

目标：把"固定链路的可视化"升级为"可重构的信号图"。

### 6.1 动态信号图（解决 D6）
- Rust 侧：节点图描述（拓扑排序、每节点参数块、连接缓冲），支持任意顺序与并联；保持零分配与实时安全。
- 前端：连线拖拽、断开/重连、多选框选、对齐吸附、缩放平移、撤销/重做。
- **验收**：osc→fx→filter 与 filter→fx 顺序切换听感明显不同；16 声部性能不回归。

### 6.2 节点内联编辑
- 常用旋钮/开关直接渲染在节点上（可折叠为"紧凑/展开"两档），减少打开面板的频次。
- **验收**：节点内改动与详情面板双向同步。

### 6.3 布局与场景
- 信号流布局预设、分享（URL/文件）、场景切换（演奏模式 A/B 段落 + pad 触发）。
- **验收**：布局导入导出、场景切换无缝。

### 6.4 多实例引擎（解决 D5，可选）
- 若做 A/B 双引擎对比或分层，需要把 `ENGINE` 单例与 arena 改为可实例化。
- **验收**：两套引擎独立运行、互不干扰；内存预算可控。

---

## 7. P3 — 可访问性、国际化与分发（持续）

- **无障碍**：全键盘导航、ARIA live 播报（和弦/预设/状态）、屏幕阅读器流程、WCAG 2.2 AA 审计、Lighthouse a11y ≥ 95。
- **主题**：多套配色 + 跟随系统 + 高对比度审计；色盲友好校验。
- **国际化**：新增日/韩/西语；复数、日期、数字格式；RTL 预留。
- **文档**：rustdoc、架构图、贡献指南、CHANGELOG、短视频教程。
- **分发**：版本化 CDN + 回滚、分享短链/二维码、可选账号与云同步（离线优先）。
- **曲库合规（解决 D4）**：内置曲目替换为公版曲库 + 用户导入，或取得授权并标注。

---

## 8. 验收指标（Definition of Done）

| 维度 | 指标 |
| :--- | :--- |
| 音频 | 无 NaN/Inf；16 声部 + 满效果不硬削波；iPhone 11 级设备 16 声部 CPU < 40% |
| 性能 | 4G 首屏可交互 < 2s；WASM gzip < 200 KB；交互响应 < 100 ms |
| 质量 | E2E 覆盖 Chromium/WebKit/Firefox；每预设渲染哈希快照；视觉回归通过 |
| 无障碍 | 关键流程纯键盘可完成；Lighthouse a11y ≥ 95 |
| 稳定 | 连续演奏 30 分钟无爆音/内存增长；PWA 更新不中断声音 |

---

## 9. 风险与决策

1. **版权（D4）**：现代影视/游戏曲目仅作短片段示范，建议 P0 内替换或明确声明；长期用公版曲库 + 用户导入。
2. **iOS 音频延迟**：AudioWorklet + 蓝牙设备延迟较高，提供缓冲/延迟提示与建议（有线/关闭蓝牙）。
3. **动态图重构成本（D6）**：先用 feature flag 渐进实现，保证固定链路路径不回退。
4. **移动端性能（D10）**：画布动画已支持离屏暂停与降帧；进一步按设备档位自适应（低端机 20 fps）。
5. **单例引擎（D5）**：除非需要多实例，否则保持单例以换取零分配与实时安全；P2 再评估。

---

## 10. 进度与下一步

**v1.8.0 已完成**
- P0.1 连续参数块速率 one-pole 平滑（~20 ms，首次下发即吸附，避免加载预设时渐变），覆盖振荡器/滤波器/包络/LFO/效果参数；新增 3 个 Rust 回归测试。
- P0.2 主输出链：软削波 + 新增前馈峰值限制器（上限 0.95、2 ms 攻击、150 ms 释放），16 声部满共振不硬削；DSP 基线不变。
- P0.7 `scripts/verify-budget.mjs` 性能预算门禁（dist 总量、首屏 JS/CSS gzip、WASM gzip、lamejs 必须为懒加载 chunk），已接入 `npm run verify`。

**下一步（按优先级）**
1. **P0.3 撤销/重做扩展**：布局、录制片段、导入、预设操作纳入命令式历史。
2. **P0.4 播放器增强**：A/B 段落循环、节拍器、录制量化、轨道独奏。
3. **P0.5 WebKit E2E**：Playwright 增加 WebKit/Firefox 项目，Safari 回归自动化。
4. **P0.6 WASM 流式编译**：`compileStreaming` 预热 + 失败回退，压缩首屏。
5. **D4 曲库合规**：替换为公版曲库 + 用户导入，或取得授权。
6. 随后进入 **P1**：按 5.1 Unison → 5.4 调制扩展 → 5.6 MPE/MIDI Learn 的顺序推进（听感收益最大）。

> P0 剩余项建议一个迭代内收尾并发布 v1.8.1；P2 立项前先做动态信号图技术预研（Rust 侧原型 + 性能评估）。

---

## 11. 交接说明（下一窗口从这里开始）

**当前版本**：**v2.0.4**，已部署到 <https://synth.wangda.today/> 并核对过（线上
`assets/index-*.js` 哈希与本地 `dist/index.html` 一致）；`npm run verify` 全绿（Rust 225 · Vitest 454（57 文件） ·
全量 Chromium E2E 141 passed / 10 skipped（含 `performance.spec.ts` 的启动预算与三负载 fps 守卫）· 视觉基线 48 张 / `test:visual` 10 passed · 预设指纹 **81 × 2**（1× 与 2× 都硬门禁，`verify:ci` 会校验脚本真的存在）· DSP 指纹 rms 0.030735 / 2× 0.030852）。
**体积：dist 阈值 1562 KB（实测 1558.3，余量 ~3.7 KB）、首屏 JS **125** KB gzip（实测 123.75，P11.2 已把 P10.2 那 2 KB 账结清并多降 8.9 KB）、最大 WASM gzip 75 KB（实测 74.2，本批未动）。**

**下一阶段计划见 [`docs/NEXT-PLAN-2.md`](NEXT-PLAN-2.md)**（P9–P12、23 个批次，含由实测新立的 P9.1c 与 P9.6）。
当前进度：NEXT-PLAN-2 已交付 **P9.1a ✅ v1.99.0**、**P9.1c 硬同步重启对齐 ✅ v1.101.0**、**P9.1b 普通振荡器带限 ✅ v1.102.0**、**P9.3 每节点效果参数 ✅ v1.103.0**、**P10.3 录音 take 二期 ✅ v1.104.0**、**P11.1 `wasm-opt -Oz` ✅ v1.105.0**、**P9.4 图模式过采样 + 逐节点 PDC ✅ v1.106.0**、**P10.1 时间线多选 ✅ v1.107.0**、**P11.3+P11.4 视觉与指纹门禁 ✅ v1.108.0**、**P10.2 片段编排二期 ✅ v1.109.0**、**P11.2 i18n 结构性拆分 + P11.5 启动门禁 ✅ v1.110.0**（首屏 JS gzip 134.65 → **123.75 KB**，阈值 136 → **125 KB**）、**P9.8/1C 采样二次修 ✅ v2.0.4**（相位表少最后一行的 31 dB bug 修掉；地板 −86.4 dB、全键盘最差 −81.0、带宽全拿回；**没动阈值**，靠 `read_wrapped` 省回 71 gzip 字节）、**P12.1 教学与练习 ✅ v2.0.3（P12 收官）**（纯逻辑评分：公式可手算；正确 100/错误 0；懒 chunk，首屏只 +0.1 KB）、**P12.2 内容包 ✅ v2.0.2**（10 条新预设展示双滤波/位粉碎/过采样/图内调制 + 5 首公版曲；指纹 81→91 而既有 81 条逐字节未变；压缩工厂表把空间买回来）、**P12.6 SW 版本握手 ✅ v2.0.2**（横幅报「将要安装的版本」；首屏 +0.1 KB）、**P9.8 采样修到 −70.5 dB + arena 8→12 MiB ✅ v2.0.1**（真因是逐级抽取而非录音带宽；P9.7 的 18 MB 估计错了 8 倍——实际 2.23 MB；装不下走可见拒绝；**体积未涨**）、**fxwire 跨引擎连线命中 ✅ v2.0.1**（真因是虚线间隙在 Gecko/WebKit 上是死画布；headless WebKit 该用例 **40.7 s 通过**）、**webkitraf headless WebKit ✅ v2.0.1**（rAF 其实会触发——是应用页在无合成器下交不出帧；帧无关交互让单次点击 8–30 s → 39–57 ms；`playwright.config.ts` 一行未改）、**v2.0.0 里程碑（P10 创作与工作流二期收官）**：**P10.4 工程管理 ✅ v2.0.0**（多套工程 + `.gs1proj` + 配额降级；dist 记账 rebase 1568→1595、首屏 JS 124.0/125）、**P10.5 曲库公版合规 ✅ v2.0.0**（移除 **9 首**在版权曲目——计划外多找到 `croatian`/`mario` 两首；逐首来源/许可标注；分因拒绝的导入；首屏 gzip −8 B）、**P9.7 波表/采样插值器 ✅ v1.113.0**（波表 ≥1 kHz **−25.8…−46.7 → 最差 −98.0 dB**；采样 −29.9 → −33.4，到 −60 需 ≈18 MB ⇒ 边界登记 §一.16；指纹重录、真的只变 4 条、DSP 基线未动；dist 记账 rebase 1568）、**P11.6 平台矩阵 ✅ v1.113.0**（nightly display 列 + 生成式趋势、DEVICE-TESTING 32 个可勾选项；**并行轨道提前交付**）、**P12.4 分发与回滚 ✅ v1.113.0**（保留 N=5 + 一键回滚 + 演练；**并行轨道提前交付**）、**P9.6 滤波器共振 × 新振荡器 ✅ v1.112.0**（计划前提被配对实测否掉：真因是 BLEP 查表 f32 舍入，P9.1c 的姊妹案例；1/150 → 0/150，**无基线重录**）、**P9.5 波表/采样带限复核 ⚠️ v1.111.0**（**验收线未达标**：实测 **−25.8…−46.7 dB**，根因是线性插值；已加门禁钉住实测地面，两个 wasm 与 v1.110.0 **逐字节相同**）、**P9.2 瞬态整形 ✅ v1.98.0**；
下一批是 **P9.6 滤波器共振 × 新振荡器交互**（C7 + 工厂 res 0.05 约 1/150 场景读到 −54.3 dB；优先走「起音斜坡」这条**不改音色**的路，改工厂默认值那条会重录全部指纹、须先报我）。**已知新账**（三项）：**P9.6 滤波器共振 × 新振荡器交互**（C7 + res 0.05 约 1/150 场景读到 −54.3 dB）；**节点参数区编号重叠**（`FX_NODE_OUT_GAIN+1 == FX_DELAY_MIX` 等）；**视觉套件不在 CI 所以基线会漂移**（P11.3 开工时它本来就是红的，10 张过期基线已补录——改 UI 的批次请自觉跑 `npm run test:visual`）。

**每批的纪律**（不要绕过）：实现 + 单元/E2E 测试 + `npm run verify` 全绿 + 小步提交 + `npm run package`
+ 部署并用**生产域名**核对线上资源 + 中文文档与更新记录同步；音频批次必须有**时域与频域**两边的断言。

**版本号里程碑（用户已定 2026-09-13，2026-09-14 达成）**：**`v2.0.0` 已于 2026-09-14 发布**——P10 创作与工作流二期收官
（P10.1 多选、P10.2 片段编排、P10.3 take、P10.4 工程管理、P10.5 曲库合规），线上 `assets/index-dM-vM01X.js` 已核对。
此后 P11/P12 从 **`v2.0.1`** 重新累加（P9.8 扩 arena → v2.0.1、P12.6 SW 握手 → v2.0.2、P12.1/P12.2 顺延）。
**并行开发（用户 2026-09-14 指示）**：多个 agent 在各自 worktree/分支上并行开发、由父代理合并后发布，所以**版本列是预计**，实际以发布顺序为准（v1.113.0 = P9.7+P11.6+P12.4，v2.0.0 = P10.4+P10.5）。规则见 `/.tmp/parallel-dev.md`。
**已拍板（2026-09-14）**：波表/采样高音区带限未达标（−25.8…−46.7 dB）**立独立批次 P9.7 修到接近 −60 dB**，接受音色变化与全部 81 条指纹重录（协议见 NEXT-PLAN-2 §一.14 与 P9.7）；P9.6 同样获授权可改工厂默认 res。
**技术债与慢轨**（NEXT-PLAN-2 §一）：**首次全面回归扫描已完成**（v1.112.0 冻结树，独立 worktree）：除一个跨引擎用例真 bug 外全绿；§一.15 的 bench 疑点**已判定为环境假红**（安静窗口 4/4 judged、p50 1197–1199 µs、0/2250 超预算）；该跨引擎 bug（`fxgraph.spec.ts` 在 Firefox/WebKit 确定性失败、自 v1.101.0 起）正在修（轨道 `fxwire`）。**慢轨只在需要时跑**：全面回归每 3–4 个版本一次，或按用户要求；`Firefox/WebKit` **只在慢轨测**（用户明确），日常批次只跑 chromium。
三条相容性红线由门禁自动兜底，改引擎前先看它们：`npm run test:dsp` 的 `rms 0.030735`、`verify:dsp:2x` 的 `0.030852`（默认音色逐样本不变），
以及 `verify:presets` / `verify:presets:2x` 的 **81 个预设指纹 × 2**（改预设必须 `presets:update -- --reason` 显式确认）。

**已知事项**：Cloudflare 对首页 HTML 有边缘缓存，部署后若线上仍是旧资源，用带参数地址或应用内「检查更新」；
`wrangler` 的 OAuth token 在 `~/.zshrc`（bash 需先 `eval` 那一行）。**生产域名是 `synth.wangda.today`**
（Worker `shiny-sky-9ea0` 的自定义域名）——不要拿旧的 `*.pages.dev` 主机名核对，它现在返回 Cloudflare 的请求
元数据 JSON（HTTP 200，但内容不是站点），见 `docs/notes/release.md`。本机缺 WebKit/Firefox 系统库，
多浏览器 e2e 需要 CI 或 `install-deps`；本地 WebKit 一律走 `npm run test:e2e:webkit:wayland`。
