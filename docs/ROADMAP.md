# GS-1 项目路线图 / Roadmap

> 基线版本：**v1.8.0**（本文档保留原始规划）。实时进度见下方「进度看板」。
>
> **进度看板（截至 v1.78.0）**
>
> | 阶段 | 项目 | 状态 |
> | :--- | :--- | :--- |
> | P0 | 4.1 参数平滑 / 4.2 主输出保护 / 4.3 撤销·重做扩展 / 4.4 播放器 A-B 循环与节拍器 / 4.7 性能预算门禁 | ✅ 已完成 |
> | P0 | 4.5 多浏览器 E2E（**CI 增加 WebKit/Firefox 作业**，本机缺系统库）/ 4.6 WASM **边下载边编译**（不可用时自动回退） | ✅ v1.51.0 |
> | P1 | 5.1 Unison / 5.2 每声部 LFO 与调制扩展 / 5.3 扩展滤波（CMB 梳状、FRM 共振峰）/ 5.4 MPE / 5.5 CC Learn / 5.6 微调律（含 .scl 导入）/ 5.7 效果与混响（含波表、效果路由、卷积） | ✅ 已完成 |
> | P2 | 布局场景 ✅ / 多实例引擎 ✅ / 采样导入 ✅ / 多轨 + 时间线（播放·混音·时间线交互）✅ / **任意拓扑节点图** | 🔶 仅剩节点图（v2.0 计划，3 批） |
> | P3 | 无障碍、更多语言、文档 | ⏸ 暂缓（按需求约定不做 a11y） |
>
> **本会话新增（v1.15 → v1.38.1）**：lookahead 限制器与响度管理 · 混响重写 · 真立体声与每振荡器滤波 ·
> CMB/FRM 滤波 · 噪声类型（白/粉/棕）· 微调律（平均/纯律/毕氏/中庸 + .scl 导入）· CC Learn · MPE 每音弯音 ·
> 力度曲线 · 撤销扩展 · A-B 循环与节拍器 · 音频设置面板与启动预热 · 版本显示与更新检查 ·
> 导出响度归一化 + 无损 WAV + 导出暂停播放 · **时域+频域双域音质门禁** · 低通重写（修复 wasm 特有的每块丢样本）·
> wasm 全局构造函数隐患根治 · 预设响度统一（47→4 dB）+ 10 个新音色 + 4 首新曲 · 更新记录面板。
>
> **下一阶段完整计划见 [`docs/NEXT-PLAN.md`](NEXT-PLAN.md)**（v1.79.0 起）：
> P5 工作流补完（时间线编辑二期 / 片段编排 / tempo map / 录音 take）→ P6 声音（FM·PM 与环形调制 / 硬同步与 sub /
> 滤波补全 / 效果补强 / 过采样开关）→ P7 节点图二期与内存模型（延迟·卷积多实例 / 图内调制 / 图模板）→
> P8 工程与质量（视觉回归 / 预设指纹 / 解析器模糊测试 / 体积与启动预算 / 一键发布）。
> **本地 WebKit E2E 统一走 `npm run test:e2e:webkit:wayland`（headless Weston）**，桌面会话用
> `test:e2e:webkit:desktop`；nightly 自动选择 Weston → Xvfb → headless 并在记录里注明。
>
> **执行队列（按收益排序，逐批交付；每批都要测试 + verify + 打包）**

| 批次 | 内容 | 状态 |
| :--- | :--- | :--- |
| 1 | A3 混响替换（阻尼 / 预延迟 / 宽度 / 湿声配平） | ✅ v1.14.0 |
| 2 | A2 lookahead 限制器 + 真峰值/响度/GR 表 + 导出 −1 dBFS 规范化 + A9 音质门禁 | ✅ v1.15.0 |
| 3 | A6 调制扩展：矩阵 8 槽 + 双极深度、AFTER/RANDOM/KEY 源、PAN/RES 目标、MIDI 触后 | ✅ v1.16.0 |
| 4 | A6 合成能力：**Unison 1–7 子声部 + SPREAD**（复音按层数折算） | ✅ v1.17.0 |
| 4a | A6 续：每声部（poly）LFO + **RETRIG / ONE SHOT** 模式 | ✅ v1.18.0 |
| 4b | P0.4 播放器：A/B 循环、节拍器、预备拍（量化/独奏随多轨批次） | ✅ v1.19.0 |
| 4b | A1 抗混叠：**重估后并入波表批次**——实测混叠 −57.7 dB（不可闻），而 2× 过采样预期仅再降约 6 dB 却要 +60% CPU；改由 A6.2 的 mipmap 波表振荡器（成本更低、抗混叠更好）统一解决 | 🔁 已改排期 |
| 5 | A4 每振荡器**真立体声**（PAN 不同即自动切换每振荡器独立滤波 + 等功率声像） | ✅ v1.20.0 |
| 5a | A4 续：**Comb 梳状谐振器**（Rust 每声部延迟线，噪声→有音高） | ✅ v1.22.0 |
| 5b | A4 续：**Formant 元音共振峰**滤波类型（三带通，A–E–I–O–U 滑动） | ✅ v1.28.0 |
| 5c | A4 续：SEM 风格滤波（现有 SVF 已覆盖 12dB 多模，优先级下调） | ⏸ 暂缓 |
| 6 | A6.2 **波表振荡器**（五张谐波表 + 每八度 mipmap 抗混叠，PW 选表） | ✅ v1.41.0 |
| 6b | A6.2 续：**单周期文件导入**（任意音频 → 自动找周期/平均 → mipmap 表，16 bit 持久化，导出同步渲染） | ✅ v1.45.0 |
| 7a | A5 续：**延迟重做**（乒乓交叉反馈、反馈环内阻尼、Rust 实现取代 Soundpipe） | ✅ v1.46.0 |
| 7b | A5 续：**效果链路可重排 + 并联送出**（同时覆盖 P2 的动态信号图：顺序即图） | ✅ v1.47.0 |
| U2 | **设置独立入口 + 分组重排**（顶栏同级入口；下拉按内容宽度，不再拉伸）；**iPhone/iPad 横竖屏 6 组 E2E** | ✅ v1.54.0 |
| U0 | 修复「重开标签页后无声音、启动键一闪而过」：启动门禁只在**引擎真正就绪**后收起（含 Chrome 自动播放策略下的回归用例） | ✅ v1.53.2 |
| U1 | **预设库底部排版重做**：等宽网格 + 字段成行对齐；设置随列表滚动，不再占用固定高度（含 E2E 防回归） | ✅ v1.53.1 |
| B1 | **双实例引擎**：第二套完整参数（消息传输 + 独立平滑）、每声部实例标记、Layer / Split / 力度窗口路由；默认单层，旧音色不变 | ✅ v1.53.0 |
| U5 | **英文排版加固**：固定槽位单行省略、顶栏/模块头可换行、过长英文缩写；新增「切英文后手机+桌面无溢出」E2E | ✅ v1.64.0 |
| U4 | **手机竖屏顶栏分行**（音色名完整一行、视图切换独占一行）+ **启动页重做**（logo/版本、更小按钮、缓动动画、尊重减弱动效） | ✅ v1.63.0 |
| U3 | **移动端体验**：手机竖屏音色横幅并入顶栏行、启动按钮重做、设置抽屉与播放器层控件的触屏尺寸（≥44px，含 E2E 断言） | ✅ v1.59.0 |
| C1a | **多轨基础**：格式 1 MIDI 保留轨道分层（轨道名 + 每轨音符），钢琴卷帘仍编辑合并列表 | ✅ v1.57.0 |
| C1b | **分层播放 + 每层静音/独奏**（播放中即时生效，单轨行为不变） | ✅ v1.58.0 |
| C1b2 | **分轨 MIDI 导出**（分层导出 format 1：指挥轨 + 每层一轨，带轨道名；单轨仍 format 0） | ✅ v1.60.0 |
| C1b3 | **每层音量**（滑杆；0 也保留音符，只降到最小力度） | ✅ v1.61.0 |
| C1b4 | **混音随曲目保存**（静音/独奏/每层音量跨重载与切歌保持；随曲库 schema 校验） | ✅ v1.62.0 |
| G2 | **卷积负载摊平**：每个 hop 的分段乘加分散到 hop 内的块（只有无法提前做的变换仍集中在边界块；基准新增 IR 行并断言其余 7 个块成本互相接近） | ✅ v1.69.0 |
| A1a | **节点图引擎（A1）**：6 个效果节点 + 干声源 + 输出汇的前馈图（每节点 2 路输入、带增益、可扇出），索引序即拓扑序、回向连线忽略即无环；`FX_GRAPH=1` 时走图、默认仍走旧链，`gs_fx_graph_sync` 从旧链自动映射且**逐样本一致**（Rust + WASM 门禁） | ✅ v1.72.0 |
| A2a | **路由图编辑器（A2）**：画布（端口拖线 / 点输出再点输入 / 点线断开）+ 列表视图（手机默认，键盘可达）、节点内联（效果类型/开关/并联/混合/增益）、三处入口、随音色保存（含 E2E 与手机视口） | ✅ v1.73.0 |
| B5 | **整首编曲随分享码**：曲目以 MIDI 字节随码传输、层混音一并带上；过长自动降级为 `.gs1song` 文件（可被预设库导入）；顺带修复「外部改混音后层条不刷新」 | ✅ v1.74.0 |
| A2b | **路由图卡片可拖动摆放**（位置进工作区、连线跟随、重置布局；参数批量提交减少重绘） | ✅ v1.76.0 |
| A2c | **连线的可视化编辑与键盘可达**：点选连线（误点不断开）、连线上直接调增益、非单位增益显示百分比标签、端口键盘操作（Tab + 回车）、连线本身可聚焦并用回车选中（含 aria-label 说明来源/目标/增益） | ✅ v1.77.0 |
| B6 | **分享码压缩**：带曲目的链接先用 `CompressionStream('deflate-raw')` 压缩（五分钟曲目 36,390 → 1,516 字符，约 96% 更小），无该 API 时回退明文；纯音色码与 `.gs1song` 格式不变，旧链接仍可读 | ✅ v1.77.0 |
| A2d | **效果节点多实例**：算法混响与四个插入效果**每个节点一套状态**（C 桥接层加 slot 维度、混响改为每节点一份）；延迟/卷积因内存上限仍单实例，第二个同类节点直通、编辑器标「已占用」并禁用；修掉「同一效果两个节点共用状态」的隐患 | ✅ v1.78.0 |
| C14 | **时间线直接编辑音符**：缩略时间线加「编排 / 音符」两种模式（音符模式：点选、拖动改位置、拖右缘改长度、双击删除、1/16 吸附、边界限制，外加一排微调按钮给手机用）；文档与撤销历史搬到共享会话 `src/state/roll.ts`，与钢琴卷帘**同一份数据、同一套撤销**（卷帘里 Ctrl+Z 能撤销时间线上的改动，应用级撤销也能，因为每次编辑都进 `store` 历史）；编辑随曲目即时落盘，内置曲目自动存副本；顺带修掉「卷帘保存多轨文件会压成单轨」 | ✅ v1.80.0 |
| C11 | **界面每帧成本**：画布改为按需重绘（示波器/频谱/迷你波形/滤波曲线/电平表只在数据或参数变化时重画、渐变缓存、DOM 只在文本变化时写）、去掉 7 处全屏 `backdrop-filter`、启动页动画改用 opacity 与静态光晕；Chromium 空闲 7.5 → 60 fps（音频运行中同样 60 fps），本机全量 E2E 8.7 → 4.2 分钟，并新增「音频运行时 > 20 fps」的 E2E 守卫 | ✅ v1.79.0 |
| C12 | **本机 WebKit 标准跑法定为 headless Weston**（`npm run test:e2e:webkit:wayland`，比 Xvfb 快一倍、有 `/dev/dri` 时走 GPU），nightly 自动按 Weston → Xvfb → headless 选择并记录显示栈；同时更正早期结论：慢的根因是应用页每帧成本，不是内核（空白页三内核均 60 fps） | ✅ v1.79.0 |
| C13 | **一键发布**：`npm run release -- <version>`（版本与更新记录校验 → `verify` → E2E → 打包 → 部署 → 线上 hash 核对并重试 → 打 tag，失败即停，`--dry-run` 可演练）；同一校验作为 `verify:release` 进本地门禁与 CI，杜绝「版本与更新记录脱节」进 main（见 `docs/notes/release.md`） | ✅ v1.79.0 |
| C10 | **体积继续拆分**：播放器面板改为按需 chunk 且首次打开才挂载（初始 JS 153.3 → **150.8 KB gzip**） | ✅ v1.78.0 |
| C9 | **门禁在繁忙机器上不再假红**：`bench` 按 `loadavg` 判定宿主是否过载（过载只报正确性、跳过计时并说明原因）；Playwright 默认超时 45→60 s；WebKit/Firefox 项目开 `reducedMotion` + 单独超时 + 重试；修掉两个跨引擎测试缺陷（Chromium 专用启动参数害 WebKit 起不来、浮动键盘挡住模块按钮） | ✅ v1.76.0 |
| C8 | **真正的夜间跑**：仓库新增 `nightly` 作业（`on.schedule`，`verify-ci` 守住它）+ 本机 `npm run nightly`（WebKit 核心子集 + `xvfb-run --headed`、锁、日志轮转、`docs/notes/nightly.md` 记录）+ systemd 用户定时器模板 | ✅ v1.76.0 |
| C7 | **本机 WebKit 可跑**：依赖装好后定位到「headless WebKit 不触发 rAF → Playwright 点击稳定性检查挂死」，给出 `xvfb-run … --headed` 跑法（`npm run test:e2e:webkit:headed`），记录在 `docs/notes/compat.md` | ✅ v1.75.0 |
| C6 | **工程项收尾**：`cargo clippy` 门禁（correctness/suspicious/perf，CI 同步）、`npm run bench:long` 60 秒长时基准（含内存/arena 断言）、更新提示显示本次更新内容 | ✅ v1.75.0 |
| G4 | **Firefox 启动卡死修复**：`resume()` 可能永不 settle，启动改为限时等待 + 「点按恢复」提示（含回归 E2E 与 `docs/notes/compat.md`） | ✅ v1.72.0 |
| G3 | **持续负载基准**（`npm run bench`，写入 `docs/notes/performance.md` 并进门禁；当前均值 9.1% 预算） | ✅ v1.66.0 |
| B4 | **分层音色进入预设/导出/分享**（带层才带路由；无层不覆盖对方的层） | ✅ v1.65.0 |
| C1b5 | **每层声像**（逐音符立体声位置：引擎按声部带 pan，滑杆 + 随曲目保存 + 测试） | ✅ v1.70.0 |
| C1b5b | **导出带上层混音**：MIDI（静音/独奏/音量→力度/偏移→时间/声像→CC10）、WAV/MP3（同一套混音）与播放完全一致 | ✅ v1.71.0 |
| C1c | **时间线第一步**：每层缩略时间线（音符块 + 播放头，纯展示） | ✅ v1.67.0 |
| C1c2 | **时间线交互第一步**：点按定位播放位置 + 拖拽整层时间偏移（随曲目保存，测试齐全） | ✅ v1.68.0 |
| C1c3 | 时间线续：拖拽裁剪音符长度、与钢琴卷帘双向联动 | ⏳ |
| B3 | **分享链接携带第二层**（带层时附带、不带时长度不变且不覆盖对方的层） | ✅ v1.56.0 |
| B2 | **双实例界面**：设置里选 1/2 决定面板编辑哪一层（store 层做到实例感知，所有面板自动跟随）、按键分配单层/叠加/分区 + 分界音、工作区持久化、E2E | ✅ v1.55.0 |
| W0 | 修复离线缓存把托管重定向（`/index.html` → `/`）交回浏览器导致的「第二次访问打不开」（Safari/Chrome） | ✅ v1.52.1 |
| W1 | **曲库持久化**：导入的 MIDI 与录制片段写入本机（schema 版本化、上限 12 首、超限仅本会话） | ✅ v1.52.0 |
| A | **采样导入（SMP 音源）**：按八度 mipmap 抗混叠变速播放、一次性/循环/乒乓、ROOT 与循环点、16 bit 持久化 + 导出渲染 | ✅ v1.50.0 |
| F | **存档 schema 版本化与迁移**（预设/分享链接/工作区/场景/波形/IR；更新版本存档回退并保留副本） | ✅ v1.49.0 |
| 7c | A5 续：**卷积混响（IR 导入）**：分段卷积（1024 采样/段，频域乘加 + 输入谱延迟线）、单位能量归一化、混响单元 ALGO/IR 双引擎 | ✅ v1.48.0 |
| 8a | A7：**微调律**（平均/纯律/毕氏/中庸律，逐键音分表） | ✅ v1.31.0 |
| 8b | A7 续：**CC Learn**（CC→控件映射，持久化） | ✅ v1.32.0 |
| 8c | A7 续：**Scala .scl 导入**（任意音数/周期，非 12 音不折算） | ✅ v1.33.0 |
| 8d | A7 续：**MPE 每音弯音**（通道→音符路由、关闭时复位弯音） | ✅ v1.34.0 |
| 9 | P0.4 播放器增强：**A/B 循环 + 节拍器（含预备拍）** ✅ v1.19.0；录制量化、轨道静音/独奏随多轨批次做 | 🔄 部分 |
| 9b | P0.4 续：**录制量化**（1/16 · 1/8 · 1/8三连 · 1/4，仅作用于新录制） | ✅ v1.39.0 |
| 9a | **P0.3 撤销/重做扩展**：布局（折叠/视图/信号流位置/重置）、用户预设删除、片段删除全部可撤销；历史记录会话初始状态 | ✅ v1.24.0 |
| 10a | P2：**布局场景**（保存/一键切换工作区；偏好不受影响） | ✅ v1.40.0 |
| 10b | P2 核心：动态信号图（真正可重排）、节点内联编辑、多实例引擎 | ⏳ 下一批 |
| 11a | **音频设置面板**（状态/采样率/延迟/内核/负载 + 复音上限）与**启动预热** | ✅ v1.26.0 |
| 11b | 音频设置：**输出设备选择**（Chromium `setSinkId`，不支持时明确提示） | ✅ v1.42.0 |
| 11 | P0.5 多浏览器 E2E（配置已就绪，本机缺系统库）、P0.6 进一步 streaming | 🔄 部分 |
| 12a | **Web MIDI 输出**（演奏转发到外部设备，关闭时发送 all-notes-off） | ✅ v1.43.0 |
| 12b | 多轨/时间线、采样导入、预设迁移 | ⏳ 下一批 |
| 13a | A8：**噪声类型**白/粉/棕（频域斜率验证：−0.07 / −3.08 / −5.99 dB/oct） | ✅ v1.35.0 |
| 13b | A8：**力度曲线**（线性/柔和/强硬，仅作用于实时演奏） | ✅ v1.38.0 |
| 13c | A8：**偷声策略**（已实现：优先偷"已释放且最轻"的声部，年龄仅作平局判定）+ 新增策略测试 | ✅ v1.38.1 |
| 13d | A8 剩余细节：滤波/调制微调 | ⏳ 下一批 |

**音质专项（A1–A9，v1.13.3 起）**
>
> | 项 | 内容 | 状态 |
> | :--- | :--- | :--- |
> | A1 | 抗混叠测量：C7 锯齿混叠相对信号 **−57.7 dB**（wasm）/ −150 dB（native，两者差异来自 FMA 与相位舍入，属次采样级差异，待过采样时一并复核）；DRIVE THD 1.1% → 4.2%（奇次谐波，属模拟味） | ✅ 已量化；过采样列为下一项 |
> | A2 | 主总线不再削波；**128 采样（约 2.7 ms）lookahead 限制器**真正生效（线性起音 / 150 ms 释放），末级为 0.82 以下逐位透明的软限制器；导出按 −1 dBFS 规范化 | ✅ 已完成 |
| A2b | 真峰值 / 短时响度 / 限制器衰减表（工作线程上报 → 监视器显示） | ✅ 已完成 |
> | A3 | 混响换为自研 Freeverb 结构（阻尼 / 预延迟 / 立体声宽度 / 慢速调制） | ✅ 已完成（v1.14.0） |
> | A4 | 每振荡器真立体声（每声部双滤波器） | ⏳ 待做 |
> | A5 | 效果并联 / 可重排、ping-pong 延迟、卷积混响 | ⏳ 待做 |
> | A6 | Unison、波表、扩展滤波、每声部 LFO、矩阵 8 槽与双极性、S&H/随机调制源 | ⏳ 待做 |
> | A7 | MPE、CC Learn、aftertouch、微调律 | ⏳ 待做 |
> | A8 | 噪声类型、偷声策略、力度曲线、LFO 重触发等细节 | ⏳ 待做 |
| A10 | **静音尾巴快速路径**（−54 dB 以下不过滤波）：电钢/铃声类长释放音色在密集曲目下的负载从 24–27% 降到 11% | ✅ v1.23.0 |
| A11 | 过载保护按"是否已错过截止时间"反应，监视器显示 DSP 负载 | ✅ v1.23.0 |
| A15 | **负载监控误报修复**（预热按音频时长 2 秒、连续块计数判定、均值慢升快降） | ✅ v1.34.1 |
| A14 | **wasm 全局构造函数隐患根治**（类内初始化 → 每次导出调用重跑 ctor；已去初始化 + 门禁防复发） | ✅ v1.32.2 |
| A13b | 波表抗混叠纳入门禁 | ✅ v1.43.1 |
| A13c | 混叠检查扩展至全部含谐波波形（锯齿/方波/脉冲/波表，−84～−156 dB） | ✅ v1.44.1 |
| A13d | 导入表纳入门禁：导入正弦谐波 −129 dB、导入锯齿 C7 混叠 −129 dB、与内置振荡器电平一致 | ✅ v1.45.0 |
| A13e | 门禁补齐：采样音高、IR 能量归一化（+2.2 dB）、效果链顺序确实改变声音（4.6 dB）、空链为精确旁通；参数 id 拼错立即报错 | ✅ v1.51.0 |
| A13 | **音质门禁时域+频域双检**：块边界咔哒扫描（含自检）、正弦纯度（谐波 −125dB / 宽带噪声 −97.6dB）、整曲频谱平坦度 | ✅ v1.32.1 |
| A12 | **低通滤波器改用自研 Rust 24dB 梯形**（原 vendored 版本在 wasm 下每块丢样本 → 单音刺啦）；混叠 −57.7→−130.2 dB、负载 19→8% | ✅ v1.30.0 |
> | A9 | 音质门禁 `npm run verify:audio`：满负荷和弦峰值/硬限幅占比、锯齿混叠（wasm 实测 −57.7 dB）、DRIVE THD（4.3%）、最坏块 CPU（19%）全部纳入 `verify` | ✅ 已完成 |

---

## 1. 现状盘点（v1.7.1）

**音频引擎（Rust → wasm32）**
- 减法合成：2 振荡器（6 波形、失谐、脉宽、声像）、Moog 梯形低通（24 dB/oct，Huovilainen 模型）+ SVF（HP/BP/NT，12 dB/oct）、双 ADSR、双 LFO、4 路调制矩阵、混响（Soundpipe `reverbsc`）、同步延迟、合唱/镶边/移相/过载。
- 工程特性：AudioWorklet 块处理（128–1024）、k-rate 参数、8 MiB 静态 arena 零分配、SIMD + 标量双版本运行时探测、Transferable ArrayBuffer 事件、负载过高按 4 步降复音。
- 16 复音、最旧/最弱偷声 + 8 槽待定队列无咔哒重触发、等功率声像。

**前端（React 18 + TS + Vite）**
- 三种工作区：模块网格、信号流画布、演奏模式（信号流承载）。
- MIDI 播放器：16 首内置曲目、SMF 导入、走带（循环/变速/移调）、现场录制、导出 MIDI/MP3（lamejs 懒加载）。
- 和弦识别（21 种模板 + 转位）、67 个工厂预设、A/B、撤销/重做、双语、PWA 离线 + 更新、响应式 + 触屏手势 + 触觉、内置双语指南（8 章）。

**质量门禁**
- 43 个 Rust 测试、~90 个 Vitest、23 个 Playwright E2E、WASM 双版本校验、DSP 基线、ESLint、GitHub Actions。

---

## 2. 已知问题与技术债

| # | 问题 | 影响 | 归属 |
| :-- | :--- | :--- | :--- |
| D1 | 参数为 k-rate 且直接写值，无平滑 | 快速拖旋钮可能有 zipper noise | 音频 |
| D2 | 主输出无限制器/软削波 | 多复音叠加可能硬削波 | 音频 |
| D3 | 撤销/重做只覆盖 patch 参数 | 布局/录制/导入不可撤销 | 状态 |
| D4 | 内置曲目含版权片段 | 分发合规风险 | 法务/内容 |
| D5 | `static mut ENGINE` 单例 + 固定 8 MiB arena | 无法多实例/多引擎对比 | 架构 |
| D6 | 信号流是固定拓扑的可视化 | 不能真正重排处理顺序 | 架构 |
| D7 | 无 MPE / CC learn，MIDI 输入较基础 | 外接控制器体验受限 | MIDI |
| D8 | E2E 仅 Chromium | Safari/Firefox 回归靠手测 | 测试 |
| D9 | WASM 用 fetch + instantiate，非 streaming | 首屏多一次往返 | 性能 |
| D10 | 画布动画在低端机可能吃帧 | 移动端续航/流畅度 | 性能 |
| D11 | 无视觉回归与性能预算门禁 | 视觉/性能回退不可见 | 工程 |

---

## 3. 规划总览

| 阶段 | 主题 | 预计版本 | 工作量 |
| :--- | :--- | :--- | :--- |
| **P0** | 音质与稳定性、关键体验缺口 | v1.8.0 | 1–2 周 |
| **P1** | 合成能力与演奏表达扩展 | v1.9.0 / v1.10 | 1–2 个月 |
| **P2** | 真正的模块化信号图与工作流 | v2.0.0 | 2–4 个月 |
| **P3** | 可访问性、国际化、分发与生态 | v2.1+ | 持续 |

---

## 4. P0 — 音质与稳定性（v1.8.0，1–2 周）

目标：把"能弹、好听、不崩"变成"专业可用"，并补齐工程门禁。

### 4.1 参数平滑（解决 D1）✅ 已完成
- 连续参数（cutoff、level、detune、mix 等）改为 **a-rate 线性/指数斜坡**：`AudioParam.linearRampToValueAtTime` 或在 worklet 内做 one-pole 平滑（推荐后者，保持 k-rate 接口但每块内插值）。
- 离散参数（波形、开关、同步）保持块速率跳变。
- **验收**：旋钮 0→100% 快速拖动无 zipper（频谱无周期性边带）；DSP 基线不回归；新增单元测试覆盖平滑系数。

### 4.2 主输出保护（解决 D2）✅ 已完成
- 主总线加软削波（`tanh`）与可选简易限制器（前馈、快攻慢放）。
- **验收**：16 声部 + 满共振 + 满过载下输出不超 0 dBFS 硬削；THD 在可控范围；A/B 对比听感不糊。

### 4.3 完整撤销/重做（解决 D3）
- 历史栈扩展为"命令"模型：参数、布局（顺序/折叠/信号流位置）、录制片段、导入、预设保存/删除均可撤销。
- **验收**：E2E 覆盖每种操作的 undo/redo；`Ctrl/Cmd+Z` 在任意面板可用。

### 4.4 播放器增强
- A/B 段落循环（拖选区间）、节拍器、录制量化（1/16/1/8/三连）、录制倒计时、轨道静音/独奏。
- **验收**：单测（量化/循环边界）+ E2E（录制→量化→回放）。

### 4.5 多浏览器 E2E（解决 D8）
- Playwright 增加 **WebKit** 项目（Safari 引擎）与 **Firefox**；关键路径（启动、发声、导入、信号流）三浏览器跑通。
- **验收**：CI 三浏览器绿灯；Safari 特有回归（`performance`、`inset`、drop-shadow）纳入用例。

### 4.6 加载性能（解决 D9）
- `WebAssembly.instantiateStreaming` + 失败回退；首屏骨架与进度提示；`wasm-opt -Oz` 与 `--strip`。
- **验收**：4G 首屏可交互 < 2s；WASM gzip < 200 KB。

### 4.7 性能预算门禁（解决 D11）✅ 已完成
- 脚本记录：WASM/JS/CSS 体积、首屏 TTI、单块 `gs_process` 耗时、16 声部 CPU 占用。
- **验收**：超阈值 CI 失败；每次发布生成性能报告。

---

## 5. P1 — 合成能力与演奏表达（v1.9–v1.10，1–2 个月）

### 5.1 Unison / 子声部
- 每个振荡器支持 1–7 个子声部，带失谐散布、相位散布、立体声扩散；supersaw 用 7×2 声部。
- **验收**：预设"Supersaw"听感达到商业参考；CPU 在预算内（可降子声部数）。

### 5.2 波表振荡器
- 单周期波表导入/内置库、线性插值、mipmap 抗混叠；波表位置可被 LFO/矩阵调制。
- **验收**：扫波表无混叠；导入 2048 点单周期文件。

### 5.3 滤波器扩展
- 新增 SEM（12 dB 多模）、Comb、Formant；每声部可串联/并联双滤波。
- **验收**：DSP 稳定性测试（高 Q 不自激失控）；预设展示。

### 5.4 调制扩展
- 新调制源：S&H、随机、包络循环、aftertouch、力度曲线；LFO 相位重置/单次/同步；矩阵扩到 8 槽 + 双极性。
- **验收**：矩阵路由可视化连线；单元测试覆盖双极性与深度换算。

### 5.5 效果扩展
- 效果并行/串联路由与顺序拖拽、卷积混响（IR 导入）、更多失真类型、主限制器。
- **验收**：IR 导入 < 2s；效果顺序切换无爆音。

### 5.6 MPE 与 MIDI Learn（解决 D7）
- MPE（每音弯音/压力/滑音）；CC 映射可编辑并持久化；MIDI learn 一键绑定。
- **验收**：外接 MPE 控制器 E2E；映射导入导出。

### 5.7 微调律
- Scala `.scl`/`.kbm` 导入、内置平均律/纯律/五度相生律。
- **验收**：音高偏差在 ±1 音分内；键盘映射正确。

---

## 6. P2 — 真正的模块化信号图（v2.0.0，2–4 个月）

目标：把"固定链路的可视化"升级为"可重构的信号图"。

### 6.1 动态信号图（解决 D6）
- Rust 侧：节点图描述（拓扑排序、每节点参数块、连接缓冲），支持任意顺序与并联；保持零分配与实时安全。
- 前端：连线拖拽、断开/重连、多选框选、对齐吸附、缩放平移、撤销/重做。
- **验收**：osc→fx→filter 与 filter→fx 顺序切换听感明显不同；16 声部性能不回归。

### 6.2 节点内联编辑
- 常用旋钮/开关直接渲染在节点上（可折叠为"紧凑/展开"两档），减少打开面板的频次。
- **验收**：节点内改动与详情面板双向同步。

### 6.3 布局与场景
- 信号流布局预设、分享（URL/文件）、场景切换（演奏模式 A/B 段落 + pad 触发）。
- **验收**：布局导入导出、场景切换无缝。

### 6.4 多实例引擎（解决 D5，可选）
- 若做 A/B 双引擎对比或分层，需要把 `ENGINE` 单例与 arena 改为可实例化。
- **验收**：两套引擎独立运行、互不干扰；内存预算可控。

---

## 7. P3 — 可访问性、国际化与分发（持续）

- **无障碍**：全键盘导航、ARIA live 播报（和弦/预设/状态）、屏幕阅读器流程、WCAG 2.2 AA 审计、Lighthouse a11y ≥ 95。
- **主题**：多套配色 + 跟随系统 + 高对比度审计；色盲友好校验。
- **国际化**：新增日/韩/西语；复数、日期、数字格式；RTL 预留。
- **文档**：rustdoc、架构图、贡献指南、CHANGELOG、短视频教程。
- **分发**：版本化 CDN + 回滚、分享短链/二维码、可选账号与云同步（离线优先）。
- **曲库合规（解决 D4）**：内置曲目替换为公版曲库 + 用户导入，或取得授权并标注。

---

## 8. 验收指标（Definition of Done）

| 维度 | 指标 |
| :--- | :--- |
| 音频 | 无 NaN/Inf；16 声部 + 满效果不硬削波；iPhone 11 级设备 16 声部 CPU < 40% |
| 性能 | 4G 首屏可交互 < 2s；WASM gzip < 200 KB；交互响应 < 100 ms |
| 质量 | E2E 覆盖 Chromium/WebKit/Firefox；每预设渲染哈希快照；视觉回归通过 |
| 无障碍 | 关键流程纯键盘可完成；Lighthouse a11y ≥ 95 |
| 稳定 | 连续演奏 30 分钟无爆音/内存增长；PWA 更新不中断声音 |

---

## 9. 风险与决策

1. **版权（D4）**：现代影视/游戏曲目仅作短片段示范，建议 P0 内替换或明确声明；长期用公版曲库 + 用户导入。
2. **iOS 音频延迟**：AudioWorklet + 蓝牙设备延迟较高，提供缓冲/延迟提示与建议（有线/关闭蓝牙）。
3. **动态图重构成本（D6）**：先用 feature flag 渐进实现，保证固定链路路径不回退。
4. **移动端性能（D10）**：画布动画已支持离屏暂停与降帧；进一步按设备档位自适应（低端机 20 fps）。
5. **单例引擎（D5）**：除非需要多实例，否则保持单例以换取零分配与实时安全；P2 再评估。

---

## 10. 进度与下一步

**v1.8.0 已完成**
- P0.1 连续参数块速率 one-pole 平滑（~20 ms，首次下发即吸附，避免加载预设时渐变），覆盖振荡器/滤波器/包络/LFO/效果参数；新增 3 个 Rust 回归测试。
- P0.2 主输出链：软削波 + 新增前馈峰值限制器（上限 0.95、2 ms 攻击、150 ms 释放），16 声部满共振不硬削；DSP 基线不变。
- P0.7 `scripts/verify-budget.mjs` 性能预算门禁（dist 总量、首屏 JS/CSS gzip、WASM gzip、lamejs 必须为懒加载 chunk），已接入 `npm run verify`。

**下一步（按优先级）**
1. **P0.3 撤销/重做扩展**：布局、录制片段、导入、预设操作纳入命令式历史。
2. **P0.4 播放器增强**：A/B 段落循环、节拍器、录制量化、轨道独奏。
3. **P0.5 WebKit E2E**：Playwright 增加 WebKit/Firefox 项目，Safari 回归自动化。
4. **P0.6 WASM 流式编译**：`compileStreaming` 预热 + 失败回退，压缩首屏。
5. **D4 曲库合规**：替换为公版曲库 + 用户导入，或取得授权。
6. 随后进入 **P1**：按 5.1 Unison → 5.4 调制扩展 → 5.6 MPE/MIDI Learn 的顺序推进（听感收益最大）。

> P0 剩余项建议一个迭代内收尾并发布 v1.8.1；P2 立项前先做动态信号图技术预研（Rust 侧原型 + 性能评估）。

---

## 11. 交接说明（下一窗口从这里开始）

**当前版本**：v1.40.0（提交 `27f2b7c`），已部署；`npm run verify` 与全部 e2e 均为绿。

**下一批**：波表已接线完成（v1.41.0）；后续可做**单周期文件导入**。**已完成：表生成器 `crates/synth-core/src/dsp/wavetable.rs`**——5 个谐波配方、
每八度一层的 **mipmap**（2048…8 采样，共 9 级）、按音高选层、线性插值；带 4 条单测（每级频谱在其自身 Nyquist 之上无能量、
选层不会混叠且不浪费、长度与归一化、相位回绕与插值）。**待接线**：

1. `crates/synth-core/src/dsp/wavetable.rs`（新建）：由**谐波配方**生成单周期表，并按八度生成 **mipmap 级**（2048/1024/…/32 采样），每一级只含该级 Nyquist 以下的谐波——**抗混叠由构造保证**，无需额外过采样；`sample(phase, level)` 线性插值。
2. `crates/synth-core/src/params.rs`：新增 `Wave::Wavetable`（映射到 `daisy_id() => None`，即 **Rust 侧渲染**，与噪声同路径）与配方参数（建议**一个共享配方参数**，省去两个 id）；同步 `PARAM_COUNT`。
3. `crates/synth-core/src/engine.rs`：在 `render_oscillator` 的 `None` 分支里按波表渲染，需要**每声部相位状态**（`wt_phase: [[f32; 2]; MAX_VOICES]`），并在 `gs_voice_reset` 路径复位；unison 可先不支持并注明。
4. TS 侧：`src/audio/params.ts`（id 与名称）、`worklet-processor.js`（**PARAMS 描述符表必须同步**，否则值到不了 DSP——本轮踩过这个坑）、`WAVES`/`WAVE_ICONS`/`i18n`、OSC 模块的配方式选择。
5. 测试：① `dsp::wavetable` 单测——每级频谱在谐波上限以上无能量（带宽受限）、基频正确、输出有界；② 引擎级**频域**测试——高音区谐波之间无混叠能量（沿用 `verify-audio.mjs` 的 FFT 手法）；③ 门禁新增一项"波表在高音区不产生混叠"。
6. 完成后：更新记录（只说改了什么）、`docs/USER-GUIDE.md`，然后一条命令 `npm run release -- <version>`（校验版本与更新记录 → `verify` → E2E → 打包 → 部署 → 线上 hash 核对 → 打 tag，见 `docs/notes/release.md`）。

**之后的队列**：A5 效果路由/并联 + 卷积混响（IR 导入）→ P2 10b 动态信号图与节点内联编辑 → 多轨/时间线、采样导入、Web MIDI 输出 → P0.5 多浏览器 E2E（需 `playwright install-deps`，配置已就绪）、P0.6 进一步 streaming。

**已知事项**：Cloudflare 对首页 HTML 有边缘缓存，部署后若线上仍是旧资源，用带参数地址或应用内「检查更新」；`wrangler` 的 OAuth token 在 `~/.zshrc`（bash 需先 `eval` 那一行）。本机缺 WebKit/Firefox 系统库，多浏览器 e2e 需要 CI 或 `install-deps`。
